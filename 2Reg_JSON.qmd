---
title: "2Reg report"
format:
  html:
    df-print: paged
    toc: true
    number-sections: true
toc: true
crossref:
  chapters: true
editor: visual
execute:
  echo: false
  warning: false
  message: false
  fig.align: center
  dev: png
  cache: false
---

```{r}

#| label: setup
#| cache: false
#| include: false

suppressPackageStartupMessages({
  library(readxl)
  library(dplyr)
  library(tidyr)
  library(purrr)
  library(stringr)
  library(tibble)
  library(openxlsx)
  library(lubridate)
  library(gtsummary)
  library(flextable)
  library(officer)
  library(ggplot2)
  library(rlang)
})

# Файлы-источники
registry_file <- "registry2-accepted-records.json"
doctors_file  <- "doctors.json"
orgs_file     <- "organizations.json"

#report_file  <- "Отчет_все_записи_12.01.2026-2.xlsx"
dict_file    <- "2Reg_Dictionary_tidy.xlsx"
patients_out <- "2Reg_patients_tidy.xlsx"
ha_out       <- "2Reg_HA_tidy.xlsx"

REPORT_TZ <- "UTC"

fmt_int <- function(x) formatC(as.integer(round(x)), format = "f", digits = 0)
fmt_1   <- function(x) formatC(x, format = "f", digits = 1)
fmt_2   <- function(x) formatC(x, format = "f", digits = 2)

save_xlsx <- TRUE

# Ниже начинается основной ETL-пайплайн:
# 1) читаем словарь и JSON-источники;
# 2) извлекаем данные пациента и процедуры гемосорбции;
# 3) нормализуем даты, единицы измерения и производные переменные;
# 4) при необходимости сохраняем два "tidy"-файла в Excel.
```

# ETL

```{r}

#| label: etl
#| cache: false
#| include: false

# Ожидается, что эти переменные заданы снаружи:
# report_file, dict_file, patients_out, ha_out, save_xlsx

# ------------------------- helpers -------------------------
# Небольшие служебные функции, которые будут много раз использоваться ниже.
# Идея простая: один раз описать типовые операции (очистка текста, разбор чисел,
# дат, единиц измерения), а потом вызывать их в основном коде.

# Приводит текст к "нормальной" форме:
# - переводит в character;
# - заменяет неразрывные пробелы и разные варианты тире;
# - убирает лишние пробелы;
# - пустые строки превращает в NA.
# Это базовая функция очистки почти для всех текстовых полей.
norm_txt <- function(x){
  x <- as.character(x)
  x <- str_replace_all(x, "\u00A0", " ")
  x <- str_replace_all(x, "[\u2212\u2013\u2014]", "-")
  x <- str_squish(x)
  x[x == ""] <- NA_character_
  x
}

# Преобразует русскую подпись временной точки вида
# "Точка [-12;0] часов" -> "m12h_0h".
# Такой короткий код удобнее использовать в фильтрах, графиках и таблицах.
tp_code <- function(s){
  s <- norm_txt(s); if (is.na(s)) return(NA_character_)
  # единицы (часы/сутки) определяем по части строки с временной точкой
  tp_part <- str_extract(s, "Точка\\s*\\[[^\\]]+\\].*$")
  if (is.na(tp_part)) tp_part <- s
  tl <- str_to_lower(tp_part)
  u <- if (str_detect(tl, "час|\\bч\\b|\\bч\\.")) {
    "h"
  } else if (str_detect(tl, "сут|дн|день")) {
    "d"
  } else {
    "h"
  }

  m <- str_match(tp_part, "Точка\\s*\\[\\s*([-+]?\\d+)\\s*;\\s*([-+]?\\d+)\\s*\\]")
  if (is.na(m[1,1])) return(NA_character_)
  f <- function(z) if (z < 0) paste0("m", abs(z), u) else paste0(z, u)
  paste0(f(as.integer(m[1,2])), "_", f(as.integer(m[1,3])))
}

# Приводит разные текстовые обозначения пропуска к единому виду NA:
# "нет данных", "нет значения", "n/a", пустая строка и т.д.
clean_na <- function(x){
  x <- as.character(x)
  x <- str_replace_all(x, "[\r\n]", " ")
  x <- str_squish(x)
  xl <- str_to_lower(x)
  x[xl %in% c("нет значения","нет данных","n/a","na")] <- NA_character_
  x[x == ""] <- NA_character_
  x
}

# Пытается извлечь из строки одно числовое значение.
# Например:
# "12,3 мг/л"   -> 12.3
# "5 | комментарий" -> 5
# Если число не найдено, вернёт NA.
to_num <- function(x){
  x <- clean_na(x)
  x <- str_split_fixed(x, "\\s*\\|\\s*", 2)[,1]
  m <- str_extract(x, "[-+]?\\d+(?:[\\.,]\\d+)?")
  as.numeric(str_replace_all(m, ",", "."))
}

# Разбирает строку лабораторного значения на:
# - числовую часть;
# - единицу измерения.
# При impute_ineq = TRUE умеет грубо обрабатывать неравенства:
#   "<10" -> 5, ">10" -> 20.
# Это используется только там, где такая замена заранее согласована.
split_val_unit <- function(x, impute_ineq = FALSE){
  x <- clean_na(x)
  x <- str_split_fixed(x, "\\s*\\|\\s*", 2)[,1]
  m <- str_match(x, "^\\s*([<>]=?|≤|≥)?\\s*([-+]?\\d+(?:[\\.,]\\d+)?)\\s*(.*)$")
  op <- str_trim(m[,2])
  val <- suppressWarnings(as.numeric(str_replace_all(m[,3], ",", ".")))
  unit <- str_squish(m[,4]); unit[unit == ""] <- NA_character_
  if (impute_ineq) val <- case_when(
    op %in% c("<","<=","≤") ~ val/2,
    op %in% c(">",">=","≥") ~ val*2,
    TRUE ~ val
  )
  list(value = val, unit = unit)
}

# Ставит рядом колонку значения и соответствующую колонку единиц измерения:
# например lactate и lactate_unit.
# Так итоговый датафрейм проще читать глазами и проверять вручную.
reorder_units <- function(df, bases){
  bases <- intersect(bases, names(df))
  fixed <- intersect(c("pat_record_id","timepoint"), names(df))
  pairs <- unlist(lapply(bases, function(b){
    u <- paste0(b, "_unit")
    if (u %in% names(df)) c(b, u) else b
  }), use.names = FALSE)
  used <- unique(c(fixed, pairs))
  rest <- setdiff(names(df), used)
  df %>% select(any_of(fixed), any_of(pairs), all_of(rest))
}

# Длительность в часы: "HH:MM" / "HH:MM:SS" -> numeric hours, иначе число трактуем как часы.
# "0:00" (и варианты с пробелами) даёт 0.
# Переводит длительность в часы.
# Поддерживает:
# - строку "HH:MM" или "HH:MM:SS";
# - уже числовое значение, которое трактуется как часы.
# На выходе всегда numeric (количество часов).
dur_to_hours <- function(x){
  x <- clean_na(x)
  x <- str_split_fixed(x, "\\s*\\|\\s*", 2)[,1]

  m <- str_match(x, "^\\s*(\\d{1,3})\\s*:\\s*(\\d{1,2})(?::\\s*(\\d{1,2}))?\\s*$")
  is_time <- !is.na(m[,1])

  out <- rep(NA_real_, length(x))
  if (any(is_time)) {
    hh <- as.numeric(m[is_time,2])
    mm <- as.numeric(m[is_time,3])
    ss <- suppressWarnings(as.numeric(m[is_time,4])); ss[is.na(ss)] <- 0
    out[is_time] <- hh + mm/60 + ss/3600
  }
  if (any(!is_time)) out[!is_time] <- to_num(x[!is_time])

  # Явно нормализуем -0 к 0 (на всякий случай)
  out[!is.na(out) & abs(out) < 1e-12] <- 0
  out
}

# ------------------------- datetime helpers -------------------------

# Универсальный парсер дат/времени: строки разных форматов + Excel-числа.
# tz оставляем "UTC" как "нейтральную" шкалу времени.
# Универсальный парсер даты/времени.
# Нужен потому, что в источниках дата может быть:
# - Excel-числом;
# - строкой "дд.мм.гггг";
# - строкой с датой и временем в разных форматах.
# Возвращает POSIXct, чтобы с датами потом можно было безопасно считать интервалы.
parse_dt <- function(x, tz = "UTC") {
  x <- clean_na(x)
  x <- str_replace_all(x, "\u00A0", " ")
  x <- str_squish(x)
  x[x == ""] <- NA_character_

  out <- rep(as.POSIXct(NA, tz = tz), length(x))

  # Excel serial numbers (дата как число)
  x_num <- suppressWarnings(as.numeric(str_replace_all(x, ",", ".")))
  is_num <- !is.na(x_num) & str_detect(x, "^\\d+(?:[\\.,]\\d+)?$")
  if (any(is_num)) {
    out[is_num] <- as.POSIXct(x_num[is_num] * 86400, origin = "1899-12-30", tz = tz)
  }

  idx <- which(!is_num & !is.na(x))
  if (length(idx)) {
    out[idx] <- suppressWarnings(lubridate::parse_date_time(
      x[idx],
      orders = c("dmy HMS","dmy HM","dmy", "ymd HMS","ymd HM","ymd", "mdy HMS","mdy HM","mdy"),
      tz = tz
    ))
  }
  out
}

# Добавляет новую datetime-колонку (new) из исходной (src), если src есть; иначе создаёт NA.
# Удобный "обёрточный" помощник:
# берёт текстовую колонку src, парсит её в дату-время и записывает в колонку new.
# Если исходной колонки нет, всё равно создаёт new, но заполняет её NA.
# Это помогает держать одинаковую структуру таблицы.
add_dt <- function(df, src, new, tz = "UTC") {
  if (src %in% names(df)) {
    df[[new]] <- suppressWarnings(parse_dt(df[[src]], tz = tz))
  } else {
    df[[new]] <- as.POSIXct(NA, tz = tz)
  }
  df
}

# ------------------------- dictionary -------------------------
# Читаем словарь переменных.
# Словарь нужен для двух вещей:
# 1) понять, какие поля относятся к patients / ha;
# 2) потом подставлять русские названия переменных в таблицы и графики.

dict <- read_excel(dict_file, col_types = "text") %>%
  mutate(across(everything(), as.character))

# dataset обязателен 
if (!("dataset" %in% names(dict))) stop("В словаре должна быть колонка dataset (patients_tidy / HA_tidy).")
if (!all(c("short","ru") %in% names(dict))) stop("В словаре должны быть колонки short и ru.")
if (!("kind" %in% names(dict))) dict$kind <- NA_character_

dict <- dict %>%
  transmute(
    dataset = norm_txt(dataset),
    short   = norm_txt(short),
    ru      = norm_txt(ru),
    kind    = norm_txt(kind)
  ) %>%
  mutate(
    dataset = case_when(
      str_to_lower(dataset) %in% c("patients_tidy","patients","patient") ~ "patients",
      str_to_lower(dataset) %in% c("ha_tidy","ha") ~ "ha",
      TRUE ~ str_to_lower(dataset)
    )
  )

# Словарь для patients_tidy: только static/time
dict_pat <- dict %>%
  filter(dataset == "patients") %>%
  filter(kind %in% c("static","time"))

ds <- filter(dict_pat, kind == "static")
dt <- filter(dict_pat, kind == "time")
ru_to_short <- setNames(dt$short, dt$ru)

# Словарь для HA_tidy: для подписей
dict_ha <- dict %>%
  filter(dataset == "ha") %>%
  filter(!is.na(short), short != "") %>%
  group_by(short) %>%
  summarise(ru = first(na.omit(ru)), .groups = "drop")

# --------------------- JSON extraction  ----------------------
# Ниже начинаем работать с JSON-источниками.
# В них данные лежат не в табличном виде, а как вложенные списки,
# поэтому сначала читаем их как list-of-lists, а потом вручную извлекаем нужные поля.

# fallback: если Quarto работает из другой папки
# Если Quarto запущен не из той папки, пытаемся найти файл в /mnt/data.
# Это страховка от ошибок вида "файл не найден" при рендеринге отчёта.
pick_path <- function(p) {
  if (file.exists(p)) return(p)
  alt <- file.path("/mnt/data", p)
  if (file.exists(alt)) return(alt)
  stop("Файл не найден: ", p)
}

registry_file <- pick_path(registry_file)
doctors_file  <- pick_path(doctors_file)
orgs_file     <- pick_path(orgs_file)

# Чтение JSON (как list-of-lists)
# Читаем JSON БЕЗ автоматического упрощения в data.frame,
# потому что структура вложенная и нам важно сохранить её как список.
read_json_list <- function(path) jsonlite::fromJSON(path, simplifyVector = FALSE)

# scalar-safe character extractor: NULL/length-0 -> NA_character_
chr1 <- function(x) {
  if (is.null(x) || length(x) == 0) return(NA_character_)
  as.character(x[[1]])
}

doctors <- read_json_list(doctors_file)
orgs    <- read_json_list(orgs_file)
reg_raw <- read_json_list(registry_file)

# Lookups
# Здесь собираем небольшие справочники:
# - кто создал запись;
# - из какой организации врач.
# Потом эти таблицы просто присоединим к основному реестру.

chr1 <- function(x) {
  if (is.null(x) || length(x) == 0) return(NA_character_)
  as.character(x[[1]])
}

doctors_lu <- purrr::map_dfr(doctors, function(x) {
  tibble::tibble(
    createdBy = chr1(x$id),
    organizationId = chr1(x$organizationId),
    record_author = stringr::str_squish(paste(chr1(x$lastName), chr1(x$firstName), chr1(x$middleName)))
  )
})

orgs_lu <- purrr::map_dfr(orgs, function(x) {
  tibble::tibble(
    organizationId = chr1(x$id),
    organization   = chr1(x$shortName)
  )
})

# Реестр: только accepted registry2
# Из большого JSON оставляем только записи нужного реестра и только в статусе accepted.
# После этого каждая строка reg_tbl = одна принятая запись пациента + вложенный блок data.

reg_tbl <- purrr::map_dfr(reg_raw, function(r) {
  tibble::tibble(
    pat_record_id = chr1(r$id),
    registryId    = chr1(r$registryId),
    status        = chr1(r$status),
    createdBy     = chr1(r$createdBy),
    createdAt     = chr1(r$createdAt),
    updatedAt     = chr1(r$updatedAt),
    data          = list(r$data)
  )
}) %>%
  dplyr::filter(registryId == "registry2", status == "accepted") %>%
  dplyr::left_join(doctors_lu, by = "createdBy") %>%
  dplyr::left_join(orgs_lu, by = "organizationId")

# ---------- helpers for nested extraction ----------
# Эти функции нужны именно для работы со вложенными JSON-полями,
# где элемент может отсутствовать, быть NULL или иметь неожиданную форму.
# Безопасно достаёт вложенное поле из списка.
# Если поле отсутствует или пустое, возвращает .default вместо ошибки.
pluck0 <- function(x, ..., .default = NA) {
  v <- purrr::pluck(x, ..., .default = .default)
  if (is.null(v) || length(v) == 0) .default else v
}

chr0 <- function(x) {
  if (is.null(x) || length(x) == 0) return(NA_character_)
  clean_na(x)
}

num0 <- function(x) {
  if (is.null(x) || length(x) == 0) return(NA_real_)
  to_num(x)
}

# Склеивает отдельные поля даты и времени в одну строку.
# Если времени нет, возвращает только дату.
combine_date_time <- function(date, time) {
  date <- clean_na(date); time <- clean_na(time)
  dplyr::case_when(
    !is.na(date) & !is.na(time) ~ paste(date, time),
    !is.na(date) ~ date,
    TRUE ~ NA_character_
  )
}

# Да/Нет -> "Да"/"Нет" (как в excel-версии)
# Приводит разные варианты "true/false", "1/0", "yes/no" к русскому виду "Да"/"Нет".
# Это делает категориальные поля единообразными для последующих таблиц.
yn_ru <- function(x){
  if (length(x) == 0) return(NA_character_)
  if (is.logical(x)) {
    return(dplyr::case_when(
      isTRUE(x) ~ "Да",
      identical(x, FALSE) ~ "Нет",
      TRUE ~ NA_character_
    ))
  }
  xl <- stringr::str_to_lower(stringr::str_squish(as.character(x)))
  dplyr::case_when(
    xl %in% c("true","t","1","да","yes","y") ~ "Да",
    xl %in% c("false","f","0","нет","no","n") ~ "Нет",
    TRUE ~ NA_character_
  )
}

# ISO 8601 (с таймзоной/offset) -> POSIXct UTC
# Разбор ISO-дат из JSON (например "2026-01-01T12:30:00Z").
# Сразу переводим их в POSIXct, чтобы потом можно было считать интервалы в часах.
parse_iso_utc <- function(x){
  x <- clean_na(x)
  if (all(is.na(x))) return(as.POSIXct(rep(NA, length(x)), tz = "UTC"))
  out <- suppressWarnings(lubridate::ymd_hms(x, tz = "UTC", quiet = TRUE))
  miss <- is.na(out) & !is.na(x)
  if (any(miss)) out[miss] <- suppressWarnings(lubridate::ymd_hm(x[miss], tz = "UTC", quiet = TRUE))
  out
}

# "dd.mm.yyyy ..." оставляем как есть, ISO конвертируем в "dd.mm.yyyy HH:MM"
# Приводит дату/время к человекочитаемому формату "дд.мм.гггг ЧЧ:ММ".
# Если строка уже в таком виде, оставляет её как есть.
# Если строка в ISO-формате, конвертирует в нужный формат вывода.
fmt_any_dmy_hm <- function(x, tz_out = REPORT_TZ){
  x <- clean_na(x)
  dplyr::if_else(
    is.na(x),
    NA_character_,
    dplyr::if_else(
      stringr::str_detect(x, "\\d{2}\\.\\d{2}\\.\\d{4}"),
      x,
      {
        dt <- parse_iso_utc(x)
        dt <- suppressWarnings(lubridate::with_tz(dt, tz_out))
        dplyr::if_else(is.na(dt), x, format(dt, "%d.%m.%Y %H:%M"))
      }
    )
  )
}

# То же самое, что fmt_any_dmy_hm(), но оставляет только дату без времени.
fmt_any_dmy <- function(x, tz_out = REPORT_TZ){
  x <- clean_na(x)
  dplyr::if_else(
    is.na(x),
    NA_character_,
    dplyr::if_else(
      stringr::str_detect(x, "\\d{2}\\.\\d{2}\\.\\d{4}"),
      substr(x, 1, 10),
      {
        dt <- parse_iso_utc(x)
        dt <- suppressWarnings(lubridate::with_tz(dt, tz_out))
        dplyr::if_else(is.na(dt), x, format(dt, "%d.%m.%Y"))
      }
    )
  )
}

dur_days  <- function(block) num0(pluck0(block, "общаяДлительность", "ОбщаяДлительностьСуток",  .default = NA))
dur_hours <- function(block) num0(pluck0(block, "общаяДлительность", "ОбщаяДлительностьЧасов", .default = NA))

# labs in БиохимияКрови: {значение, единица} -> "value unit"
# Извлекает числовое лабораторное значение из JSON-объекта.
# В JSON лаборатория часто хранится как список вида {значение, единица}.
# На выходе оставляем только число.
lab_num <- function(obj, impute_ineq = FALSE) {
  if (is.null(obj)) return(NA_real_)

  val <- if (is.list(obj)) pluck0(obj, "значение", .default = NA) else obj
  if (is.null(val) || (length(val) == 1 && is.na(val))) return(NA_real_)

  # в JSON иногда приходит строка вида ">100" — как в excel-версии (impute_ineq = TRUE только для PCT)
  if (is.character(val)) {
    sp <- split_val_unit(val, impute_ineq = isTRUE(impute_ineq))
    return(sp$value)
  }

  suppressWarnings(as.numeric(val))
}

# Извлекает единицу измерения из лабораторного JSON-объекта.
lab_unit <- function(obj) {
  if (is.null(obj)) return(NA_character_)
  if (!is.list(obj)) return(NA_character_)
  chr0(pluck0(obj, "единица", .default = NA_character_))
}

# Поддерживаем разные варианты "Положительный/Отрицательный результат"
# Нормализует результаты посевов к небольшому числу стандартных вариантов.
# Это важно, потому что в источнике одно и то же может быть записано по-разному.
norm_culture <- function(x){
  x <- norm_txt(x)
  dplyr::case_when(
    x %in% c("Положительный посев", "Положительный результат") ~ "Положительный посев",
    x %in% c("Отрицательный посев", "Отрицательный результат") ~ "Отрицательный посев",
    x == "Посев не проводился" ~ "Посев не проводился",
    x %in% c("Нет данных", "Нет значения") ~ NA_character_,
    TRUE ~ x
  )
}

# ---------- patients_tidy (long by timepoint) ----------
# На этом этапе превращаем одну JSON-запись пациента в "длинную" таблицу:
# одна строка = один пациент на одном таймпойнте.
# Статические данные пациента будут повторяться в каждой его строке,
# а динамические показатели будут зависеть от timepoint.
num0 <- function(x) suppressWarnings(to_num(as.character(clean_na(x))))
chr0 <- function(x) clean_na(as.character(x))

# Главная функция извлечения данных пациента.
# На входе: одна запись реестра (включая вложенный блок data).
# На выходе: tibble с одной или несколькими строками пациента по временным точкам.
extract_patient_rows <- function(pat_record_id, record_author, organization, createdAt, updatedAt, data) {
  d <- data

  # static / patient-level
  # Здесь собираем сведения, которые относятся ко всему пациенту в целом
  # и не меняются от таймпойнта к таймпойнту.
  outcome_block <- pluck0(d, "ИсходНаблюдения", .default = list())
  
vpr_applied <- pluck0(d, "ПоддержкаОрганнойДисфункции", "Вазопрессоры", "применялась", .default = NA)

outcome_block <- pluck0(d, "ИсходНаблюдения", .default = list())

organ_block <- pluck0(d, "ПоддержкаОрганнойДисфункции", .default = list())
vpr_block   <- pluck0(organ_block, "Вазопрессоры", .default = list())
mv_block    <- pluck0(organ_block, "ИВЛ", .default = list())
rrt_block   <- pluck0(organ_block, "ЗПТ", .default = list())
ecmo_block  <- pluck0(organ_block, "ЭКМО", .default = list())

static <- tibble::tibble(
  pat_record_id = chr0(pat_record_id),
  record_author = chr0(record_author),
  organization  = chr0(organization),

  # Даты записи
  # createdAt — всегда есть (дата создания записи); updatedAt может быть пустым (это нормально)
  record_date_created_raw = createdAt,
  record_date_created_dt  = parse_iso_utc(createdAt),
  record_date_created     = fmt_any_dmy_hm(createdAt),

  record_date_updated_raw = updatedAt,
  record_date_updated_dt  = parse_iso_utc(updatedAt),
  record_date_updated     = fmt_any_dmy_hm(updatedAt),

  # Backward-compat: раньше использовали record_date_accepted, но updatedAt не годится как “accepted”.
  # Оставляем как дату создания записи.
  # record_date_accepted = fmt_any_dmy_hm(createdAt),

  # Базовые сведения / анамнез
  admission_date          = chr0(pluck0(d, "ДатаГоспитализации", .default = NA_character_)),
  diagnoses               = chr0(pluck0(d, "Диагнозы", .default = NA_character_)),
  HA_indications          = chr0(pluck0(d, "ПоказанияГС", .default = NA_character_)),
  ICU_6_month_history     = chr0(pluck0(d, "ПребываниеВОРИТВПоследние6Мес", .default = NA_character_)),
  from_other_ICU_transfer = chr0(pluck0(d, "ПереводИзДругогоОРИТ", .default = NA_character_)),

  age_yr = num0(pluck0(d, "ВозрастЛет", .default = NA)),
  age_m  = num0(pluck0(d, "ВозрастМесяцев", .default = NA)),
  sex    = chr0(pluck0(d, "Пол", .default = NA_character_)),
  BMI    = num0(pluck0(d, "ИМТ", .default = NA)),

  charlson_index = num0(pluck0(d, "ИндексЧарлсон", .default = NA)),

  # Патогены
  pat_gram_positive = chr0(norm_culture(pluck0(d, "Грамположительные", .default = NA_character_))),
  pat_gram_negative = chr0(norm_culture(pluck0(d, "Грамотрицательные", .default = NA_character_))),
  pat_bacteremia    = chr0(norm_culture(pluck0(d, "Бактериемия", .default = NA_character_))),
  pat_fungal        = chr0(norm_culture(pluck0(d, "Грибы", .default = NA_character_))),

  # Исходы
  outcome = chr0(pluck0(outcome_block, "Исход", .default = NA_character_)),
  HA_efficency_CGI_E = chr0(pluck0(outcome_block, "ШкалаЭффективностиГемосорбции", .default = NA_character_)),

  # Даты ICU / антибиотики
  ICU_in_datetime = chr0(combine_date_time(
    pluck0(d, "ДатаВремяПоступленияОРИТ_Дата",  .default = NA_character_),
    pluck0(d, "ДатаВремяПоступленияОРИТ_Время", .default = NA_character_)
  )),
  ICU_out_or_death_date = chr0(combine_date_time(
    pluck0(outcome_block, "ДатаВремяВыпискиОРИТСмерти_Дата",  .default = NA_character_),
    pluck0(outcome_block, "ДатаВремяВыпискиОРИТСмерти_Время", .default = NA_character_)
  )),
  antibiotics_start_datetime = fmt_any_dmy_hm(pluck0(d, "ДатаВремяНачалаАнтибиотикотерапии", .default = NA_character_)),

  # Поддержка органной дисфункции (Да/Нет + детализация)
  vasopressors_if_used    = yn_ru(pluck0(vpr_block, "применялась", .default = NA)),
  vasopressors_start      = fmt_any_dmy_hm(pluck0(vpr_block, "датаВремяНачала", .default = NA_character_)),
  vasopressors_resolution = fmt_any_dmy_hm(pluck0(vpr_block, "ДатаОкончательногоРазрешения", .default = NA_character_)),
  vasopressors_breaks     = yn_ru(pluck0(vpr_block, "наличиеПерерыва", .default = NA)),
  vasopressors_dur_days   = dur_days(vpr_block),
  vasopressors_dur_hours  = dur_hours(vpr_block),

  MV_if_used    = yn_ru(pluck0(mv_block, "применялась", .default = NA)),
  MV_start      = fmt_any_dmy_hm(pluck0(mv_block, "датаВремяНачала", .default = NA_character_)),
  MV_resolution = fmt_any_dmy_hm(pluck0(mv_block, "ДатаОкончательногоРазрешения", .default = NA_character_)),
  MV_breaks     = yn_ru(pluck0(mv_block, "наличиеПерерыва", .default = NA)),
  MV_dur_days   = dur_days(mv_block),
  MV_dur_hours  = dur_hours(mv_block),

  RRT_if_used    = yn_ru(pluck0(rrt_block, "применялась", .default = NA)),
  RRT_start      = fmt_any_dmy_hm(pluck0(rrt_block, "датаВремяНачала", .default = NA_character_)),
  RRT_resolution = fmt_any_dmy_hm(pluck0(rrt_block, "ДатаОкончательногоРазрешения", .default = NA_character_)),
  RRT_breaks     = yn_ru(pluck0(rrt_block, "наличиеПерерыва", .default = NA)),
  RRT_dur_days   = dur_days(rrt_block),
  RRT_dur_hours  = dur_hours(rrt_block),

  ECMO_if_used    = yn_ru(pluck0(ecmo_block, "применялась", .default = NA)),
  ECMO_start      = fmt_any_dmy_hm(pluck0(ecmo_block, "датаВремяНачала", .default = NA_character_)),
  ECMO_resolution = fmt_any_dmy_hm(pluck0(ecmo_block, "ДатаОкончательногоРазрешения", .default = NA_character_)),
  ECMO_breaks     = yn_ru(pluck0(ecmo_block, "наличиеПерерыва", .default = NA)),
  ECMO_dur_days   = dur_days(ecmo_block),
  ECMO_dur_hours  = dur_hours(ecmo_block)
)

  # timepoints union across blocks
  # Временные точки могут встречаться не во всех разделах одновременно
  # (например, есть в "КлиническаяОценка", но нет в "БиохимияКрови").
  # Поэтому сначала собираем общий список всех точек, которые вообще есть у пациента.
  tp_names <- unique(c(
    names(pluck0(d, "КлиническаяОценка", .default = list())),
    names(pluck0(d, "КлеткиКрови", .default = list())),
    names(pluck0(d, "БиохимияКрови", .default = list()))
  ))
  tp_names <- tp_names[grepl("^Точка", tp_names)]
  if (!length(tp_names)) {
    return(static %>% mutate(timepoint = NA_character_))
  }

  # Для каждой найденной временной точки достаём клинические показатели,
  # клетки крови и биохимию, после чего собираем одну строку dyn.
  dyn <- purrr::map_dfr(tp_names, function(tp_ru) {
    tp <- tp_code(tp_ru)

    cl <- pluck0(d, "КлиническаяОценка", tp_ru, .default = list())
    kc <- pluck0(d, "КлеткиКрови", tp_ru, .default = list())
    bi <- pluck0(d, "БиохимияКрови", tp_ru, .default = list())

    # биохимия: значение (numeric) + единицы (character) отдельными колонками
    lactate_obj <- pluck0(bi, "лактат", .default = NULL)
    creat_obj   <- pluck0(bi, "креатинин", .default = NULL)
    alb_obj     <- pluck0(bi, "альбумин", .default = NULL)
    pct_obj     <- pluck0(bi, "прокальцитонин", .default = NULL)
    crp_obj     <- pluck0(bi, "С_реактивный_белок", .default = pluck0(bi, "С_реактивный белок", .default = NULL))
    dd_obj      <- pluck0(bi, "Д_Димер", .default = NULL)
    fib_obj     <- pluck0(bi, "Фибриноген", .default = NULL)
    bili_obj    <- pluck0(bi, "Общий_билирубин", .default = NULL)

    tibble::tibble(
  timepoint = tp,

clin_assess_date = fmt_any_dmy_hm(pluck0(cl, "датаПроведения", .default = NA_character_)),
blood_cells_date = fmt_any_dmy_hm(pluck0(kc, "датаПробы", .default = NA_character_)),
blood_bio_date   = fmt_any_dmy_hm(pluck0(bi, "датаПробы", .default = NA_character_)),

      # клиническая оценка
      SOFA    = num0(pluck0(cl, "балSOFA", .default = NA)),
      VIS2020 = num0(pluck0(cl, "индексVIS2020", .default = NA)),
      avg_BP  = num0(pluck0(cl, "среднееАД", .default = NA)),
      PaFiO2  = num0(pluck0(cl, "PaO2_FiO2", .default = NA)),
      SpFiO2  = num0(pluck0(cl, "SpO2_FiO2", .default = NA)),

      # клетки крови
      leucocytes   = num0(pluck0(kc, "лейкоциты", .default = NA)),
      neutrophils  = num0(pluck0(kc, "нейтрофилы", .default = NA)),
      lymphocytes  = num0(pluck0(kc, "лимфоциты", .default = NA)),
      thrombocytes = num0(pluck0(kc, "тромбоциты", .default = NA)),

      # биохимия
      lactate         = lab_num(lactate_obj),
      lactate_unit    = lab_unit(lactate_obj),

      creatinine      = lab_num(creat_obj),
      creatinine_unit = lab_unit(creat_obj),

      albumin         = lab_num(alb_obj),
      albumin_unit    = lab_unit(alb_obj),

      procalcitonin = lab_num(pct_obj, impute_ineq = TRUE),
      procalcitonin_unit = lab_unit(pct_obj),

      C_react_protein      = lab_num(crp_obj),
      C_react_protein_unit = lab_unit(crp_obj),

      D_dimer      = lab_num(dd_obj),
      D_dimer_unit = lab_unit(dd_obj),

      fibrinogen      = lab_num(fib_obj),
      fibrinogen_unit = lab_unit(fib_obj),

      bilirubin_total      = lab_num(bili_obj),
      bilirubin_total_unit = lab_unit(bili_obj)
    )
  })


  # bind static to each timepoint row
  # Повторяем статические данные рядом с каждой динамической строкой,
  # чтобы итоговая таблица была самодостаточной и не требовала отдельного join.
  out <- dplyr::bind_cols(
    dyn,
    static[rep(1, nrow(dyn)), , drop = FALSE]
  )

  out
}

# Применяем extract_patient_rows() ко всем accepted-записям реестра
# и склеиваем результат в одну большую таблицу patients_tidy.
patients_tidy <- purrr::pmap_dfr(
  reg_tbl %>% dplyr::select(pat_record_id, record_author, organization, createdAt, updatedAt, data),
  extract_patient_rows
) %>%
  dplyr::mutate(across(where(is.character), clean_na))

# --- QC: какие id “выпали” при формировании patients_tidy ---
# Полезная проверка: убеждаемся, что после извлечения не потеряли пациентов.
# Если какие-то id отсутствуют в patients_tidy, их стоит отдельно проверить в исходном JSON.
all_ids <- unique(clean_na(reg_tbl$pat_record_id))
pt_ids  <- unique(clean_na(patients_tidy$pat_record_id))

all_ids <- all_ids[!is.na(all_ids) & all_ids != ""]
pt_ids  <- pt_ids[!is.na(pt_ids) & pt_ids != ""]

missing_ids <- setdiff(all_ids, pt_ids)

cat("registry ids:", length(all_ids), "\n")
cat("patients_tidy ids:", length(pt_ids), "\n")
cat("missing ids:", length(missing_ids), "\n")

if (length(missing_ids) > 0) {
  print(head(missing_ids, 20))
}


# фиксируем порядок таймпойнтов 
tp_levels_core <- c("m12h_0h", "48h_72h", "4d_6d", "7d_10d")
tp_levels <- c(tp_levels_core, setdiff(sort(unique(patients_tidy$timepoint)), tp_levels_core))

patients_tidy <- patients_tidy %>%
  mutate(timepoint = factor(timepoint, levels = tp_levels, ordered = TRUE))

# ---------- HA_tidy from ПроцедурыГемосорбции ----------
# Аналогично patients_tidy, но уже для процедур гемосорбции.
# Здесь одна строка = одна процедура.
# Извлекает все процедуры гемосорбции одного пациента из блока data.
# Если процедур нет, возвращает пустую таблицу.
extract_ha_rows <- function(pat_record_id, data) {
  procs <- pluck0(data, "ПроцедурыГемосорбции", .default = list())
  if (is.null(procs) || !length(procs)) return(tibble::tibble())

  ha0 <- purrr::map_dfr(procs, function(p) {
    tibble::tibble(
      pat_record_id = pat_record_id,
      HA_cartrige_type = pluck0(p, "DeviceType", .default = NA_character_),
      HA_serial_number = pluck0(p, "СерийныйНомерУстройства", .default = NA_character_),
      HA_start_datetime = combine_date_time(
        pluck0(p, "ДатаВремяНачалаГС_Дата",  .default = NA_character_),
        pluck0(p, "ДатаВремяНачалаГС_Время", .default = NA_character_)
      ),
      HA_duration = pluck0(p, "ДлительностьГС", .default = NA_character_),
      HA_avg_blood_flow = pluck0(p, "СредняяСкоростьПотока", .default = NA),
      HA_anticoagulation = pluck0(p, "Антикоагуляция", .default = NA_character_),
      HA_anticoagulation_other = pluck0(p, "АнтикоагуляцияДругое", .default = NA_character_),
      HA_other_methods = pluck0(p, "КомбинацияСДругимиЭкстракорпоральнымиМетодами", .default = NA_character_),
      HA_adverse_effects = pluck0(p, "НежелательныеЯвления", .default = NA_character_)
    )
  })

  # задаём HA_parameter как "Гемоперфузия 1/2/..." по времени старта (если старта нет — по порядку)
  ha0 <- ha0 %>%
    mutate(.start_dt_tmp = parse_dt(HA_start_datetime, tz = "UTC")) %>%
    arrange(.start_dt_tmp) %>%
    mutate(HA_parameter = paste0("Гемоперфузия ", dplyr::row_number())) %>%
    select(-.start_dt_tmp)

  ha0
}

HA_tidy <- purrr::pmap_dfr(
  reg_tbl %>% dplyr::select(pat_record_id, data),
  extract_ha_rows
) %>%
  mutate(
    HA_avg_blood_flow = to_num(HA_avg_blood_flow),
    HA_duration = round(dur_to_hours(HA_duration), 2)
  )

# Добавляем количество процедур с разными картриджами по каждому пациенту
# Это пациент-уровневые агрегаты, которые потом пригодятся в summary-таблицах.
HA_counts <- HA_tidy %>%
  mutate(.ct = str_to_upper(norm_txt(HA_cartrige_type))) %>%
  group_by(pat_record_id) %>%
  summarise(
    CT_count = sum(.ct == "CT", na.rm = TRUE),
    LPS_count = sum(.ct == "LPS", na.rm = TRUE),
    .groups = "drop"
  )

HA_tidy <- HA_tidy %>%
  left_join(HA_counts, by = "pat_record_id") %>%
  mutate(
    CT_count = if_else(is.na(CT_count), 0L, as.integer(CT_count)),
    LPS_count = if_else(is.na(LPS_count), 0L, as.integer(LPS_count))
  )

# ------------------ postprocess ------------------
# После первичного извлечения данных делаем "доводку":
# - парсим даты;
# - рассчитываем status;
# - создаём производные переменные;
# - стандартизируем единицы измерения.

# patients: datetime-колонки (создаём *всегда*, даже если исходной колонки нет)
# В dt_map перечислены текстовые колонки с датой/временем и имена,
# под которыми будут храниться их распарсенные версии.
dt_map <- c(
  ICU_in_datetime            = "ICU_in_dt",
  ICU_out_or_death_date      = "ICU_out_or_death_dt",
  antibiotics_start_datetime = "antibiotics_start_dt",
  RRT_start                  = "RRT_start_dt",
  RRT_resolution             = "RRT_resolution_dt",
  MV_start                   = "MV_start_dt",
  MV_resolution              = "MV_resolution_dt",
  clin_assess_date           = "clin_assess_dt",
  blood_cells_date           = "blood_cells_dt",
  blood_bio_date             = "blood_bio_dt"
)

# 1) сохраняем исходные значения в *_raw (только для тех колонок, которые реально есть)
# Это полезно для аудита: всегда можно сравнить исходную строку и распарсенную дату.
patients_tidy <- patients_tidy %>%
  mutate(across(any_of(names(dt_map)), ~ .x, .names = "{.col}_raw"))

# 2) добавляем распарсенные *_dt
for (src in names(dt_map)) {
  patients_tidy <- add_dt(patients_tidy, src, dt_map[[src]], tz = "UTC")
}

# 3) удаляем "оригинальные" колонки, чтобы остались только *_raw и *_dt
patients_tidy <- patients_tidy %>%
  select(-any_of(names(dt_map)))


# Основной расчёт VIS2020_0_excl (если VIS2020 есть)
# Для анализа VIS исключаем ВСЕХ пациентов, у кого на baseline (m12h_0h)
# VIS2020 отсутствует или равен 0.
if ("VIS2020" %in% names(patients_tidy)) {
  patients_tidy <- patients_tidy %>%
    group_by(pat_record_id) %>%
    mutate(
      .vis_baseline_bad = any(timepoint == "m12h_0h" & (is.na(VIS2020) | VIS2020 == 0)),
      VIS2020_0_excl = if_else(.vis_baseline_bad, NA_real_, VIS2020)
    ) %>%
    ungroup() %>%
    select(-.vis_baseline_bad)
}

# NLR: один раз на всех timepoint
if (all(c("neutrophils","lymphocytes") %in% names(patients_tidy))) {
  patients_tidy <- patients_tidy %>%
    mutate(
      NLR = if_else(
        !is.na(neutrophils) & !is.na(lymphocytes) & lymphocytes > 0,
        neutrophils / lymphocytes,
        NA_real_
      )
    )
}

# -------------------- Приведение единиц измерения + *_raw --------------------
# Некоторые лабораторные показатели могут приходить в разных единицах.
# Чтобы сравнивать их между пациентами и строить общие таблицы,
# приводим всё к одной целевой единице и сохраняем исходные значения в *_raw.
# Сохраняем исходные значения в *_raw / *_unit_raw и приводим к единому виду:
# - fibrinogen -> мг/дл
# - bilirubin_total -> мкмоль/л
# - D_dimer -> мг/л FEU

unit_norm <- function(u){
  u <- norm_txt(u)
  u <- str_to_lower(u)
  u <- str_replace_all(u, "μ", "µ")
  u <- str_replace_all(u, "\\s+", "")
  u
}

# Фибриноген -> мг/дл
if (all(c("fibrinogen","fibrinogen_unit") %in% names(patients_tidy))) {
  patients_tidy$fibrinogen_raw      <- patients_tidy$fibrinogen
  patients_tidy$fibrinogen_unit_raw <- patients_tidy$fibrinogen_unit

  fu <- unit_norm(patients_tidy$fibrinogen_unit)
  is_gdl  <- !is.na(fu) & str_detect(fu, "^(г|g)/(дл|dl)\\.?$")
  is_gl   <- !is.na(fu) & str_detect(fu, "^(г|g)/(л|l)\\.?$")
  is_mgl  <- !is.na(fu) & str_detect(fu, "^(мг|mg)/(л|l)\\.?$")
  is_mgdl <- !is.na(fu) & str_detect(fu, "^(мг|mg)/(дл|dl)\\.?$")

  patients_tidy$fibrinogen <- dplyr::case_when(
    is_gdl ~ patients_tidy$fibrinogen * 1000,  # g/dl -> mg/dl
    is_gl  ~ patients_tidy$fibrinogen * 100,   # g/l  -> mg/dl
    is_mgl ~ patients_tidy$fibrinogen / 10,    # mg/l -> mg/dl
    TRUE   ~ patients_tidy$fibrinogen
  )
  patients_tidy$fibrinogen_unit <- dplyr::case_when(
    is_gdl | is_gl | is_mgl | is_mgdl ~ "мг/дл",
    TRUE ~ patients_tidy$fibrinogen_unit
  )
}

# Общий билирубин -> мкмоль/л
if (all(c("bilirubin_total","bilirubin_total_unit") %in% names(patients_tidy))) {
  patients_tidy$bilirubin_total_raw      <- patients_tidy$bilirubin_total
  patients_tidy$bilirubin_total_unit_raw <- patients_tidy$bilirubin_total_unit

  bu <- unit_norm(patients_tidy$bilirubin_total_unit)
  is_mmol <- !is.na(bu) & str_detect(bu, "^(ммоль|mmol)/(л|l)\\.?$")
  is_umol <- !is.na(bu) & str_detect(bu, "^(мкмоль|umol|µmol)/(л|l)\\.?$")
  is_mgdl <- !is.na(bu) & str_detect(bu, "^(мг|mg)/(дл|dl)\\.?$")
  is_mgl  <- !is.na(bu) & str_detect(bu, "^(мг|mg)/(л|l)\\.?$")

  patients_tidy$bilirubin_total <- dplyr::case_when(
    is_mmol ~ patients_tidy$bilirubin_total * 1000,     # mmol/l -> µmol/l
    is_mgdl ~ patients_tidy$bilirubin_total * 17.104,   # mg/dl  -> µmol/l
    is_mgl  ~ patients_tidy$bilirubin_total * 1.7104,   # mg/l   -> µmol/l
    TRUE    ~ patients_tidy$bilirubin_total
  )
  patients_tidy$bilirubin_total_unit <- dplyr::case_when(
    is_mmol | is_umol | is_mgdl | is_mgl ~ "мкмоль/л",
    TRUE ~ patients_tidy$bilirubin_total_unit
  )
}

# D-димер -> мг/л FEU
if (all(c("D_dimer","D_dimer_unit") %in% names(patients_tidy))) {
  patients_tidy$D_dimer_raw      <- patients_tidy$D_dimer
  patients_tidy$D_dimer_unit_raw <- patients_tidy$D_dimer_unit

  du <- unit_norm(patients_tidy$D_dimer_unit)
  is_mgl  <- !is.na(du) & str_detect(du, "^(мг|mg)/(л|l)")
  is_mgdl <- !is.na(du) & str_detect(du, "^(мг|mg)/(дл|dl)")
  is_ugml <- !is.na(du) & str_detect(du, "^(мкг|µg|ug)/(мл|ml)")
  is_ugl  <- !is.na(du) & str_detect(du, "^(мкг|µg|ug)/(л|l)")
  is_ngml <- !is.na(du) & str_detect(du, "^(нг|ng)/(мл|ml)")

  is_feu  <- !is.na(du) & str_detect(du, "feu|феу")
  is_ddu  <- !is.na(du) & str_detect(du, "ddu")

  # сначала приводим к мг/л, затем (если DDU) -> FEU (×2)
  d_mgl <- dplyr::case_when(
    is_mgdl ~ patients_tidy$D_dimer * 10,      # mg/dl -> mg/l
    is_mgl  ~ patients_tidy$D_dimer,           # mg/l  -> mg/l
    is_ugml ~ patients_tidy$D_dimer,           # µg/ml -> mg/l
    is_ugl  ~ patients_tidy$D_dimer / 1000,    # µg/l  -> mg/l
    is_ngml ~ patients_tidy$D_dimer / 1000,    # ng/ml -> mg/l
    TRUE    ~ patients_tidy$D_dimer
  )
  d_mgl <- dplyr::if_else(is_ddu & !is.na(d_mgl), d_mgl * 2, d_mgl)

  patients_tidy$D_dimer <- d_mgl
  patients_tidy$D_dimer_unit <- dplyr::case_when(
    is_mgl | is_mgdl | is_ugml | is_ugl | is_ngml | is_feu | is_ddu ~ "мг/л FEU",
    TRUE ~ patients_tidy$D_dimer_unit
  )
}

# HA: datetime + нормализация типа картриджа
HA_tidy <- HA_tidy %>%
  mutate(HA_start_datetime_raw = HA_start_datetime) %>%
  add_dt("HA_start_datetime", "HA_start_dt") %>%
  mutate(
    HA_cartrige_type_std = str_to_upper(str_squish(as.character(HA_cartrige_type)))
  ) %>%
  # Аналогично: оставляем только parsed-вариант даты старта (HA_start_dt)
  select(-any_of("HA_start_datetime"))

# ------------------ HA → patient-level переменные (для patients_tidy) ------------------
# Из таблицы процедур получаем признаки уровня пациента:
# например, был ли LPS в первые 72 часа, когда была первая процедура и т.д.
# После этого присоединяем эти признаки обратно к patients_tidy.
# Цель: максимум расчётов делать здесь, в ETL, чтобы дальше использовать patients_tidy/HA_tidy напрямую.
#
# (1) is_lps_72h: был ли хотя бы один LPS-картридж в первые 72 часа от поступления в ОРИТ
# (2) ICU_to_first_LPS / ICU_to_first_CT:
#     время (в часах) от ICU_in_dt до первого по времени HA_start_dt среди LPS / CT соответственно
# (3) first_sorption_dt / first_sorption_type:
#     дата/время и тип картриджа процедуры с параметром HA_parameter == "Гемоперфузия 1"
#
# Важно:
# - все эти признаки константны внутри pat_record_id, поэтому после расчёта мы просто left_join в patients_tidy
# - если по пациенту нет ни одной записи HA, то is_lps_72h = 0, остальные переменные = NA

icu_by_id <- patients_tidy %>%
  group_by(pat_record_id) %>%
  summarise(
    ICU_in_dt = first(na.omit(ICU_in_dt)),
    .groups = "drop"
  ) %>%
  mutate(ICU_in_plus72 = ICU_in_dt + hours(72))

ha_by_id <- HA_tidy %>%
  left_join(icu_by_id, by = "pat_record_id") %>%
  mutate(
    .cart = HA_cartrige_type_std,
    .is_hp1 = str_detect(norm_txt(HA_parameter), regex("^Гемоперфузия\\s*1$", ignore_case = TRUE))
  ) %>%
  group_by(pat_record_id) %>%
  summarise(
    # 1) LPS в первые 72 часа
    is_lps_72h = as.integer(any(
      .cart == "LPS" &
        !is.na(HA_start_dt) & !is.na(ICU_in_dt) &
        HA_start_dt >= ICU_in_dt & HA_start_dt <= ICU_in_plus72
    )),

    # 2) первые даты LPS/CT
    .first_LPS_start_dt = {
      x <- HA_start_dt[.cart == "LPS"]
      if (any(!is.na(x))) min(x, na.rm = TRUE) else as.POSIXct(NA, tz = "UTC")
    },
    .first_CT_start_dt = {
      x <- HA_start_dt[.cart == "CT"]
      if (any(!is.na(x))) min(x, na.rm = TRUE) else as.POSIXct(NA, tz = "UTC")
    },

    # 3) «Гемоперфузия 1»: берём самую раннюю по времени запись
    first_sorption_dt = {
      x <- HA_start_dt[.is_hp1]
      if (any(!is.na(x))) min(x, na.rm = TRUE) else as.POSIXct(NA, tz = "UTC")
    },
    first_sorption_type = {
      idx <- which(.is_hp1 & !is.na(HA_start_dt))
      if (length(idx) == 0) NA_character_
      else .cart[idx[which.min(HA_start_dt[idx])]]
    },

    ICU_in_dt = first(na.omit(ICU_in_dt)),
    .groups = "drop"
  ) %>%
  mutate(
    # время (часы) от ICU до первых LPS/CT и до первой «Гемоперфузия 1»
    ICU_to_first_LPS = round(as.numeric(difftime(.first_LPS_start_dt, ICU_in_dt, units = "hours")), 1),
    ICU_to_first_CT  = round(as.numeric(difftime(.first_CT_start_dt,  ICU_in_dt, units = "hours")), 1),
    icu_to_HA1_hours = round(as.numeric(difftime(first_sorption_dt,   ICU_in_dt, units = "hours")), 1)
  ) %>%
  select(
    pat_record_id,
    is_lps_72h,
    ICU_to_first_LPS,
    ICU_to_first_CT,
    icu_to_HA1_hours,
    first_sorption_dt,
    first_sorption_type
  )

patients_tidy <- patients_tidy %>%
  left_join(ha_by_id, by = "pat_record_id") %>%
  mutate(
    is_lps_72h = coalesce(as.integer(is_lps_72h), 0L)
  )

# ---- patient status по таймпойнтам (in / out / dead / n/a) ----
# Здесь для каждой строки пациента определяем, находился ли он в ОРИТ на этот момент.
# - "in"   = к концу окна пациент ещё в ОРИТ;
# - "out"  = выписан из ОРИТ;
# - "dead" = умер;
# - "n/a"  = не хватает информации, чтобы надёжно решить.
# Правило:
# - если событие (ICU_out_or_death_dt) произошло ДО начала окна -> уже out/dead (по outcome)
# - если событие ВНУТРИ окна -> out/dead (по outcome)
# - если событие ПОСЛЕ окна -> in
# - если даты/исходы недостаточны -> n/a
if (all(c("timepoint", "first_sorption_dt", "ICU_out_or_death_dt", "outcome") %in% names(patients_tidy))) {

  tp_windows <- tibble::tibble(
    timepoint = c("m12h_0h", "48h_72h", "4d_6d", "7d_10d"),
    start_h   = c(-12, 48, 96, 168),
    end_h     = c(0, 72, 144, 240)
  )

  patients_tidy <- patients_tidy %>%
    dplyr::left_join(tp_windows, by = "timepoint") %>%
    dplyr::mutate(
      .hours_to_event = as.numeric(difftime(ICU_out_or_death_dt, first_sorption_dt, units = "hours")),
      .event_status   = dplyr::case_when(
        outcome == "Умер" ~ "dead",
        outcome == "Выписан из ОРИТ" ~ "out",
        outcome %in% c("Переведён в другой ОРИТ", "Продолжает находиться в ОРИТ") ~ "in",
        TRUE ~ "n/a"
      ),
      status = dplyr::case_when(
        # неизвестен таймпойнт или нет даты включения
        is.na(start_h) | is.na(first_sorption_dt) ~ "n/a",

        # нет ICU_out_or_death_dt: если outcome говорит, что пациент в ОРИТ -> in, иначе n/a
        is.na(ICU_out_or_death_dt) ~ dplyr::if_else(.event_status == "in", "in", "n/a"),

        # событие до начала окна
        .hours_to_event < start_h ~ .event_status,

        # событие внутри окна
        .hours_to_event >= start_h & .hours_to_event <= end_h ~ .event_status,

        # событие после окна -> пациент ещё in на этой точке
        .hours_to_event > end_h ~ "in",

        TRUE ~ "n/a"
      ),
      status = factor(status, levels = c("in", "out", "dead", "n/a"), ordered = TRUE)
    ) %>%
    dplyr::select(-start_h, -end_h, -.hours_to_event, -.event_status)
}

# ---- SOFA: сохраняем исходные значения и заполняем 0 при status == out ----
# Логика: если SOFA на точке неизвестна (NA) и пациент к этой точке уже OUT, то SOFA = 0.
if (all(c("SOFA", "status") %in% names(patients_tidy))) {
  patients_tidy <- patients_tidy %>%
    dplyr::mutate(
      SOFA_raw = SOFA,
      SOFA = dplyr::if_else(is.na(SOFA) & status == "out", 0, SOFA)
    )
}

# Патогены: 1 = положительный, 0 = отрицательный, N/A = посев не проводился (как отдельная категория)
culture_to_1_0_na <- function(x) {
  x <- norm_txt(x)
  dplyr::case_when(
    x %in% c("Положительный посев", "Положительный результат") ~ "1",
    x %in% c("Отрицательный посев", "Отрицательный результат") ~ "0",
    x %in% c("Посев не проводился", "Не проводился") ~ "N/A",
    TRUE ~ "N/A"
  ) |>
    factor(levels = c("1", "0", "N/A"))
}

if ("pat_gram_positive" %in% names(patients_tidy)) {
  patients_tidy$is_pat_gram_positive <- culture_to_1_0_na(patients_tidy$pat_gram_positive)
}

if ("pat_gram_negative" %in% names(patients_tidy)) {
  patients_tidy$is_pat_gram_negative <- culture_to_1_0_na(patients_tidy$pat_gram_negative)
}



if ("pat_bacteremia" %in% names(patients_tidy)) {
  patients_tidy$is_bacteremic <- culture_to_1_0_na(patients_tidy$pat_bacteremia)
}

# is_septic: 1, если есть признаки сепсиса/септ.шока по показаниям
# ИЛИ грамотрицательный посев ИЛИ бактериемия ИЛИ PCT > 10 на m12h_0h; иначе 0
if (all(c("pat_record_id", "HA_indications") %in% names(patients_tidy))) {

  if (all(c("timepoint", "procalcitonin") %in% names(patients_tidy))) {
    septic_pct_tbl <- patients_tidy %>%
      dplyr::group_by(pat_record_id) %>%
      dplyr::summarise(
        .septic_pct = any(timepoint == "m12h_0h" & !is.na(procalcitonin) & procalcitonin > 10, na.rm = TRUE),
        .groups = "drop"
      )
  } else {
    septic_pct_tbl <- patients_tidy %>%
      dplyr::distinct(pat_record_id) %>%
      dplyr::mutate(.septic_pct = FALSE)
  }

  patients_tidy <- patients_tidy %>%
    dplyr::left_join(septic_pct_tbl, by = "pat_record_id") %>%
    dplyr::mutate(
      is_septic = dplyr::if_else(
        stringr::str_detect(stringr::str_to_lower(dplyr::coalesce(HA_indications, "")), "\\bсепсис\\b|(?<!а)септическ") |
          dplyr::coalesce(as.character(is_pat_gram_negative), "") == "1" |
          dplyr::coalesce(as.character(is_bacteremic), "") == "1" |
          dplyr::coalesce(.septic_pct, FALSE),
        1L, 0L
      )
    ) %>%
    dplyr::select(-.septic_pct)
}


# Показания к гемосорбции: читаем один раз (если файл есть)
# Отдельный Excel-файл с диагнозами используется дальше только для summary-таблицы.
# Если файла нет, отчёт всё равно соберётся, просто без этого блока.
diag_file <- "2Reg_diagnoses.xlsx"
if (!file.exists(diag_file) && file.exists("/mnt/data/2Reg_diagnoses.xlsx")) {
  diag_file <- "/mnt/data/2Reg_diagnoses.xlsx"
}

diag_wide <- NULL
if (file.exists(diag_file)) {
  diag_wide <- tryCatch(
    readxl::read_excel(diag_file, sheet = 2),
    error = function(e) NULL
  )
}

# ------------------------ output --------------------------
# При save_xlsx = TRUE сохраняем две итоговые таблицы в Excel,
# чтобы ими можно было пользоваться отдельно от отчёта.

if (isTRUE(save_xlsx)) {
  write.xlsx(patients_tidy, patients_out, asTable = TRUE, overwrite = TRUE)
  write.xlsx(HA_tidy, ha_out, asTable = TRUE, overwrite = TRUE)
}
```

## PaFiO2_calc: расчёт по SpFiO2

```{r}
#| label: pafi_spfi_regression
#| cache: true
#| echo: false

# Строим простую линейную модель PaFiO2 ~ SpFiO2 на полных наблюдениях,
# то есть только там, где оба показателя реально измерены.
# Затем используем эту модель для расчёта PaFiO2 там, где исходное значение пропущено.
pafi_spfi_df <- patients_tidy %>%
  filter(!is.na(PaFiO2), !is.na(SpFiO2))

lm_pafi_spfi <- lm(PaFiO2 ~ SpFiO2, data = pafi_spfi_df)

coef_tbl <- tibble::tibble(
  term = names(coef(lm_pafi_spfi)),
  estimate = unname(coef(lm_pafi_spfi))
)

coef_tbl %>% knitr::kable(digits = 3)
cat("R² =", round(summary(lm_pafi_spfi)$r.squared, 3), "\n")

ggplot(pafi_spfi_df, aes(x = SpFiO2, y = PaFiO2)) +
  geom_point(alpha = 0.5) +
  geom_smooth(method = "lm", se = TRUE) +
  labs(x = "SpFiO2", y = "PaFiO2", title = "PaFiO2 ~ SpFiO2") +
  theme_minimal()

# Пересчёт отсутствующих PaFiO2 по предсказанию модели:
# PaFiO2_calc = PaFiO2, если есть; иначе (b0 + b1 * SpFiO2), если SpFiO2 есть.
b0 <- unname(coef(lm_pafi_spfi)[1])
b1 <- unname(coef(lm_pafi_spfi)[2])

patients_tidy <- patients_tidy %>%
  mutate(
    PaFiO2_calc = dplyr::case_when(
  !is.na(PaFiO2) ~ round(PaFiO2, 2),
  is.na(PaFiO2) & !is.na(SpFiO2) ~ round(b0 + b1 * SpFiO2, 2),
  TRUE ~ NA_real_
)
  )
```

# EDA/QC

```{r}
#| label: eda_qc
#| cache: true
#| echo: false

library(knitr)

# Этот chunk не меняет данные, а только проверяет их качество:
# ищет пропуски, необычные единицы, подозрительные диапазоны и возможные дубли.

# -------------------- 0) Паспорт --------------------
cat(
  "patients_tidy: rows =", nrow(patients_tidy),
  "| unique patients =", dplyr::n_distinct(patients_tidy$pat_record_id),
  "\n"
)

tp_counts <- patients_tidy %>%
  dplyr::count(timepoint, name = "n_rows") %>%
  dplyr::arrange(timepoint)
knitr::kable(tp_counts, caption = "Строки по таймпойнтам (patients_tidy)")

# Дубли pat_record_id × timepoint
dup_id_tp <- patients_tidy %>%
  dplyr::count(pat_record_id, timepoint, name = "n") %>%
  dplyr::filter(!is.na(pat_record_id), pat_record_id != "", n > 1) %>%
  dplyr::arrange(dplyr::desc(n))
if (nrow(dup_id_tp) > 0) {
  knitr::kable(head(dup_id_tp, 30), caption = "ДУБЛИ pat_record_id × timepoint (первые 30)")
} else {
  cat("Дублей pat_record_id × timepoint не найдено.\n")
}

# -------------------- 1) Missingness --------------------
id_cols <- intersect(c("pat_record_id", "record_author", "organization", "timepoint"), names(patients_tidy))
data_cols <- setdiff(names(patients_tidy), id_cols)

miss_overall <- patients_tidy %>%
  dplyr::summarise(dplyr::across(dplyr::all_of(data_cols), ~ mean(is.na(.)), .names = "{.col}")) %>%
  tidyr::pivot_longer(dplyr::everything(), names_to = "variable", values_to = "p_miss") %>%
  dplyr::arrange(dplyr::desc(p_miss))
knitr::kable(head(miss_overall, 30), caption = "Top-30 переменных с наибольшей долей пропусков (overall)")

miss_by_tp <- patients_tidy %>%
  dplyr::group_by(timepoint) %>%
  dplyr::summarise(
    n_rows = dplyr::n(),
    dplyr::across(dplyr::all_of(data_cols), ~ mean(is.na(.)), .names = "{.col}"),
    .groups = "drop"
  ) %>%
  tidyr::pivot_longer(
    cols = -c(timepoint, n_rows),
    names_to = "variable",
    values_to = "p_miss"
  ) %>%
  dplyr::group_by(timepoint) %>%
  dplyr::slice_max(p_miss, n = 10, with_ties = FALSE) %>%
  dplyr::ungroup() %>%
  dplyr::arrange(timepoint, dplyr::desc(p_miss))

knitr::kable(miss_by_tp, caption = "Top-10 самых “пустых” переменных в каждом таймпойнте")

all_na <- miss_overall %>% dplyr::filter(p_miss == 1)
if (nrow(all_na) > 0) {
  knitr::kable(all_na, caption = "Колонки полностью NA (кандидаты на удаление/фикс парсинга)")
}

# -------------------- 2) Аудит единиц измерения --------------------
unit_cols <- names(patients_tidy)[stringr::str_detect(names(patients_tidy), "_unit$")]
bases <- sub("_unit$", "", unit_cols)
bases <- bases[bases %in% names(patients_tidy)]

if (length(bases) > 0) {
  unit_long <- purrr::map_dfr(bases, function(b) {
    u <- paste0(b, "_unit")
    patients_tidy %>%
      dplyr::transmute(
        pat_record_id,
        timepoint,
        variable = b,
        value = .data[[b]],
        unit  = .data[[u]]
      )
  })

  unit_freq <- unit_long %>%
    dplyr::filter(!is.na(unit), unit != "") %>%
    dplyr::count(variable, timepoint, unit, name = "n") %>%
    dplyr::group_by(variable, timepoint) %>%
    dplyr::mutate(p = n / sum(n)) %>%
    dplyr::ungroup() %>%
    dplyr::arrange(variable, timepoint, dplyr::desc(n))
  knitr::kable(unit_freq, caption = "Частоты единиц измерения (variable × timepoint × unit)")

  unit_multi <- unit_long %>%
    dplyr::filter(!is.na(unit), unit != "") %>%
    dplyr::distinct(variable, unit) %>%
    dplyr::count(variable, name = "n_units") %>%
    dplyr::filter(n_units > 1)
  if (nrow(unit_multi) > 0) {
    knitr::kable(unit_multi, caption = "Переменные со СМЕШАННЫМИ единицами (кандидаты на конвертацию)")
  } else {
    cat("Смешанных единиц (по *_unit) не видно.\n")
  }

  unit_stats <- unit_long %>%
    dplyr::filter(!is.na(value), !is.na(unit), unit != "") %>%
    dplyr::group_by(variable, unit) %>%
    dplyr::summarise(
      n = dplyr::n(),
      median = median(value),
      p25 = stats::quantile(value, 0.25),
      p75 = stats::quantile(value, 0.75),
      min = min(value),
      max = max(value),
      .groups = "drop"
    ) %>%
    dplyr::arrange(variable, dplyr::desc(n))
  knitr::kable(unit_stats, caption = "Сводка значений по единицам (variable × unit)")

  ggplot2::ggplot(
    unit_long %>% dplyr::filter(!is.na(value), !is.na(unit), unit != ""),
    ggplot2::aes(x = unit, y = value)
  ) +
    ggplot2::geom_boxplot(outlier.alpha = 0.3) +
    ggplot2::facet_wrap(~ variable, scales = "free_y") +
    ggplot2::coord_flip() +
    ggplot2::labs(
      title = "Распределения по единицам (поиск смешанных масштабов)",
      x = NULL, y = NULL
    )
} else {
  cat("Колонок *_unit не найдено — аудит единиц пропущен.\n")
}


# -------------------- 2b) Конвертации единиц: до/после --------------------
conv_cols_need <- c(
  "fibrinogen_raw","fibrinogen_unit_raw","fibrinogen","fibrinogen_unit",
  "bilirubin_total_raw","bilirubin_total_unit_raw","bilirubin_total","bilirubin_total_unit",
  "D_dimer_raw","D_dimer_unit_raw","D_dimer","D_dimer_unit"
)

if (all(conv_cols_need %in% names(patients_tidy))) {

  unit_norm2 <- function(u){
    u <- as.character(u)
    u <- stringr::str_squish(u)
    u <- stringr::str_to_lower(u)
    u <- stringr::str_replace_all(u, "μ", "µ")
    u <- stringr::str_replace_all(u, "\\s+", "")
    dplyr::if_else(u %in% c("nan","na","n/a",""), NA_character_, u)
  }

  conv_long <- dplyr::bind_rows(
    patients_tidy %>%
      dplyr::transmute(
        pat_record_id, timepoint,
        variable = "fibrinogen",
        value_raw = fibrinogen_raw,
        unit_raw  = fibrinogen_unit_raw,
        value     = fibrinogen,
        unit      = fibrinogen_unit,
        target_unit = "мг/дл"
      ),
    patients_tidy %>%
      dplyr::transmute(
        pat_record_id, timepoint,
        variable = "bilirubin_total",
        value_raw = bilirubin_total_raw,
        unit_raw  = bilirubin_total_unit_raw,
        value     = bilirubin_total,
        unit      = bilirubin_total_unit,
        target_unit = "мкмоль/л"
      ),
    patients_tidy %>%
      dplyr::transmute(
        pat_record_id, timepoint,
        variable = "D_dimer",
        value_raw = D_dimer_raw,
        unit_raw  = D_dimer_unit_raw,
        value     = D_dimer,
        unit      = D_dimer_unit,
        target_unit = "мг/л FEU"
      )
  ) %>%
    dplyr::mutate(
      unit_raw_n = unit_norm2(unit_raw),
      unit_n     = unit_norm2(unit),
      converted = dplyr::case_when(
        !is.na(value_raw) & !is.na(value) & abs(value_raw - value) > 1e-9 ~ TRUE,
        !is.na(unit_raw_n) & !is.na(unit_n) & unit_raw_n != unit_n ~ TRUE,
        TRUE ~ FALSE
      )
    )

  # 1) Сколько конвертаций (по variable × timepoint)
  conv_summary <- conv_long %>%
    dplyr::group_by(variable, timepoint, target_unit) %>%
    dplyr::summarise(
      n_total = dplyr::n(),
      n_with_raw = sum(!is.na(value_raw) | !is.na(unit_raw)),
      n_converted = sum(converted, na.rm = TRUE),
      p_converted = dplyr::if_else(n_with_raw > 0, n_converted / n_with_raw, NA_real_),
      .groups = "drop"
    ) %>%
    dplyr::arrange(variable, timepoint)

  knitr::kable(conv_summary, caption = "Конвертация единиц: сколько строк было конвертировано (variable × timepoint)")

  # 2) Частоты единиц ДО/ПОСЛЕ
  unit_freq <- dplyr::bind_rows(
    conv_long %>% dplyr::transmute(variable, timepoint, stage = "raw",   unit = unit_raw),
    conv_long %>% dplyr::transmute(variable, timepoint, stage = "final", unit = unit)
  ) %>%
    dplyr::filter(!is.na(unit), unit != "") %>%
    dplyr::count(variable, timepoint, stage, unit, name = "n") %>%
    dplyr::arrange(variable, timepoint, stage, dplyr::desc(n))

  knitr::kable(unit_freq, caption = "Единицы измерения ДО/ПОСЛЕ конвертации (частоты)")

  # 3) Что НЕ привелось к целевым единицам (кандидаты на доработку)
  not_target <- conv_long %>%
    dplyr::filter(!is.na(unit), unit != "", unit != target_unit) %>%
    dplyr::count(variable, target_unit, unit, name = "n") %>%
    dplyr::arrange(variable, dplyr::desc(n))

  if (nrow(not_target) > 0) {
    knitr::kable(not_target, caption = "Единицы, которые НЕ привелись к целевым (проверьте, нужны ли доп. правила конвертации)")
  }

} else {
  cat("Конвертация единиц: raw/стандартизированные колонки не найдены — блок пропущен.\n")
}


# -------------------- 2c) Status: валидатор --------------------
if ("status" %in% names(patients_tidy)) {

  tp_windows_v <- tibble::tibble(
    timepoint = c("m12h_0h", "48h_72h", "4d_6d", "7d_10d"),
    start_h   = c(-12, 48, 96, 168),
    end_h     = c(0, 72, 144, 240)
  )

  status_tab <- patients_tidy %>%
    dplyr::filter(timepoint %in% tp_windows_v$timepoint) %>%
    dplyr::group_by(timepoint, status) %>%
    dplyr::summarise(
      n_rows = dplyr::n(),
      n_pat  = dplyr::n_distinct(pat_record_id),
      .groups = "drop"
    ) %>%
    dplyr::mutate(timepoint = factor(timepoint, levels = tp_windows_v$timepoint, ordered = TRUE)) %>%
    dplyr::arrange(timepoint, status)

  knitr::kable(status_tab, caption = "Status по таймпойнтам (строки и уникальные пациенты)")

  has_dates <- all(c("ICU_in_dt", "ICU_out_or_death_dt", "outcome") %in% names(patients_tidy))
  if (has_dates) {

    # Минимальный набор “клинических” колонок для проверки: если пациент выбыл ДО окна,
    # но в этом окне всё ещё есть измерения — это повод перепроверить исходные даты/таймпойнт.
    check_cols <- intersect(
      c("SOFA", "PaFiO2_calc", "VIS2020", "lactate", "procalcitonin", "C_react_protein",
        "D_dimer", "bilirubin_total", "fibrinogen", "creatinine"),
      names(patients_tidy)
    )

    pt_chk <- patients_tidy %>%
      dplyr::left_join(tp_windows_v, by = "timepoint") %>%
      dplyr::mutate(
        hours_to_event = as.numeric(difftime(ICU_out_or_death_dt, ICU_in_dt, units = "hours")),
        outcome_status = dplyr::case_when(
          outcome == "Умер" ~ "dead",
          outcome == "Выписан из ОРИТ" ~ "out",
          outcome %in% c("Переведён в другой ОРИТ", "Продолжает находиться в ОРИТ") ~ "in",
          TRUE ~ "n/a"
        ),
        event_before_window = !is.na(hours_to_event) & !is.na(start_h) & hours_to_event < start_h,
        event_after_window  = !is.na(hours_to_event) & !is.na(end_h) & hours_to_event > end_h,
        any_clin_data = if (length(check_cols) > 0) dplyr::if_any(dplyr::all_of(check_cols), ~ !is.na(.)) else FALSE,

        # Флаги:
        # 1) пациент выбыл ДО начала окна, но на этой точке остались измерения
        flag_data_after_exit = event_before_window & status %in% c("out", "dead") & any_clin_data,

        # 2) по outcome+датам пациент должен быть out/dead, но status остался in
        flag_status_should_be_out_dead = event_before_window & outcome_status %in% c("out", "dead") & status == "in",

        # 3) по датам событие ПОСЛЕ окна, но status почему-то out/dead (маловероятно, но проверяем)
        flag_status_should_be_in = event_after_window & outcome_status %in% c("out", "dead") & status %in% c("out", "dead")
      )

    val_tp <- pt_chk %>%
      dplyr::filter(timepoint %in% tp_windows_v$timepoint) %>%
      dplyr::group_by(timepoint) %>%
      dplyr::summarise(
        n_rows = dplyr::n(),
        n_data_after_exit = sum(flag_data_after_exit, na.rm = TRUE),
        n_status_should_be_out_dead = sum(flag_status_should_be_out_dead, na.rm = TRUE),
        n_status_should_be_in = sum(flag_status_should_be_in, na.rm = TRUE),
        .groups = "drop"
      ) %>%
      dplyr::mutate(timepoint = factor(timepoint, levels = tp_windows_v$timepoint, ordered = TRUE)) %>%
      dplyr::arrange(timepoint)

    knitr::kable(val_tp, caption = "Валидатор status: потенциальные несоответствия по таймпойнтам")

    bad_rows <- pt_chk %>%
      dplyr::filter(flag_data_after_exit | flag_status_should_be_out_dead | flag_status_should_be_in) %>%
      dplyr::transmute(
        pat_record_id, timepoint, status, outcome,
        ICU_in_dt, ICU_out_or_death_dt,
        hours_to_event,
        flags = paste0(
          ifelse(flag_data_after_exit, "data_after_exit;", ""),
          ifelse(flag_status_should_be_out_dead, "status_should_be_out/dead;", ""),
          ifelse(flag_status_should_be_in, "status_should_be_in;", "")
        )
      ) %>%
      dplyr::arrange(timepoint, pat_record_id)

    if (nrow(bad_rows) > 0) {
      knitr::kable(head(bad_rows, 30), caption = "Примеры (первые 30) строк с флагами валидатора status")
    } else {
      cat("Валидатор status: явных несоответствий не найдено.\n")
    }

  } else {
    cat("Валидатор status пропущен: не найдены ICU_in_dt / ICU_out_or_death_dt / outcome.\n")
  }

} else {
  cat("Колонка status не найдена — валидатор status пропущен.\n")
}

# -------------------- 3) Диапазоны/выбросы (numeric) --------------------
num_vars <- names(patients_tidy)[vapply(patients_tidy, is.numeric, logical(1))]
if (length(num_vars) > 0) {
  num_long <- patients_tidy %>%
    dplyr::select(timepoint, dplyr::all_of(num_vars)) %>%
    tidyr::pivot_longer(-timepoint, names_to = "variable", values_to = "value")

  num_audit <- num_long %>%
    dplyr::filter(!is.na(value)) %>%
    dplyr::group_by(variable) %>%
    dplyr::summarise(
      n = dplyr::n(),
      p01 = stats::quantile(value, 0.01),
      p50 = stats::median(value),
      p99 = stats::quantile(value, 0.99),
      min = min(value),
      max = max(value),
      n_neg = sum(value < 0),
      .groups = "drop"
    ) %>%
    dplyr::mutate(spread = dplyr::if_else(p01 == 0, NA_real_, abs(p99 / p01))) %>%
    dplyr::arrange(dplyr::desc(spread))
  knitr::kable(head(num_audit, 30), caption = "Top-30 числовых по ‘разбросу’ (p99/p01) + негативные значения")
}

# -------------------- 4) “Похоже на число, но осталось character” --------------------
char_cols <- names(patients_tidy)[vapply(patients_tidy, is.character, logical(1))]
char_cols <- setdiff(char_cols, id_cols)

looks_numeric <- function(x) {
  x <- stats::na.omit(x)
  if (length(x) < 20) return(FALSE)
  mean(stringr::str_detect(x, "\\d"), na.rm = TRUE) > 0.9
}

maybe_num <- char_cols[vapply(patients_tidy[char_cols], looks_numeric, logical(1))]
if (length(maybe_num) > 0) {
  ex <- tibble::tibble(
    variable = maybe_num,
    example = vapply(maybe_num, function(v) {
      paste(head(stats::na.omit(patients_tidy[[v]]), 3), collapse = " ; ")
    }, character(1))
  )
  knitr::kable(ex, caption = "Колонки, которые выглядят числовыми, но остались character (примеры значений)")
}

# -------------------- 5) HA: быстрый QC --------------------
if (exists("HA_tidy")) {
  cat(
    "HA_tidy: rows =", nrow(HA_tidy),
    "| unique patients =", dplyr::n_distinct(HA_tidy$pat_record_id),
    "\n"
  )

  cartridge_col <- dplyr::case_when(
    "HA_cartrige_type" %in% names(HA_tidy) ~ "HA_cartrige_type",
    "HA_cartrige_type_std" %in% names(HA_tidy) ~ "HA_cartrige_type_std",
    TRUE ~ NA_character_
  )

  if (!is.na(cartridge_col)) {
    ha_cartr <- HA_tidy %>%
      dplyr::count(type = .data[[cartridge_col]], name = "n") %>%
      dplyr::arrange(dplyr::desc(n)) %>%
      dplyr::rename(HA_cartrige_type = type)
    knitr::kable(head(ha_cartr, 20), caption = "HA: частоты типов картриджей (top-20)")
  }
}
```

# Пациенты gtsummary

```{r}
#| label: gtsummary_pat
#| cache: true
#| echo: false

# В этом чанке собираем большую пациентскую summary-таблицу из двух частей:
# 1) "верх" — одна строка на пациента на baseline (m12h_0h);
# 2) "низ" — динамика лаборатории и шкал по timepoint.
tp_top <- "m12h_0h"
tps_bottom <- c("m12h_0h", "48h_72h", "4d_6d", "7d_10d")
show_antibiotics <- FALSE  # временно скрыто

# RU-лейблы из словаря (dict_pat уже создан в ETL)
ru_map <- dict_pat %>%
  distinct(short, ru) %>%
  filter(!is.na(short), short != "", !is.na(ru), ru != "") %>%
  tibble::deframe()

ru_or <- function(key) {
  x <- unname(ru_map[[key]])
  if (is.null(x) || is.na(x) || x == "") key else x
}

unit_mode <- function(x) {
  x <- as.character(x)
  x <- x[!is.na(x) & x != ""]
  if (!length(x)) return(NA_character_)
  names(sort(table(x), decreasing = TRUE))[1]
}

lab_u <- function(base_ru, unit) {
  if (!is.na(unit) && unit != "") paste0(base_ru, " (", unit, ")") else base_ru
}

# ---------------- data: верх (только m12h_0h) ----------------
pt_top <- patients_tidy %>%
  filter(timepoint == tp_top) %>%
  distinct(pat_record_id, .keep_all = TRUE) %>%
  mutate(
    vasopressors_used = case_when(
      str_to_lower(vasopressors_if_used) == "да"  ~ "Да",
      str_to_lower(vasopressors_if_used) == "нет" ~ "Нет",
      TRUE ~ NA_character_
    ),
    vasopressors_used = factor(vasopressors_used, levels = c("Да","Нет"))
  )

if (isTRUE(show_antibiotics) && "antibiotics_start_dt" %in% names(pt_top)) {
  pt_top <- pt_top %>%
    mutate(
      antibiotics_started = if_else(!is.na(antibiotics_start_dt), "Да", "Нет"),
      antibiotics_started = factor(antibiotics_started, levels = c("Да","Нет"))
    )
}

# ---------------- HA: counts (HA1 timing уже в patients_tidy) ----------------
ha_counts <- HA_tidy %>%
  distinct(pat_record_id, CT_count, LPS_count) %>%
  mutate(
    CT_count  = coalesce(as.integer(CT_count),  0L),
    LPS_count = coalesce(as.integer(LPS_count), 0L),
    devices_total = CT_count + LPS_count
  )

pt_top <- pt_top %>%
  left_join(ha_counts, by = "pat_record_id") %>%
  mutate(
    HA1_start_dt = first_sorption_dt,

    # исключаем 0 из разбивки по типам устройств на пациента (0 -> NA)
    CT_count_pos      = if_else(!is.na(CT_count) & CT_count > 0, as.integer(CT_count), NA_integer_),
    LPS_count_pos     = if_else(!is.na(LPS_count) & LPS_count > 0, as.integer(LPS_count), NA_integer_),
    devices_total_pos = if_else(!is.na(devices_total) & devices_total > 0, as.integer(devices_total), NA_integer_),

    # ----- RRT/MV required относительно HA1_start_dt -----
    RRT_required = case_when(
      is.na(RRT_start_dt) ~ "Нет",
      is.na(HA1_start_dt) ~ NA_character_,
      HA1_start_dt >= (RRT_start_dt - hours(12)) &
        (is.na(RRT_resolution_dt) | HA1_start_dt <= RRT_resolution_dt) ~ "Да",
      TRUE ~ "Нет"
    ),
    MV_required = case_when(
      is.na(MV_start_dt) ~ "Нет",
      is.na(HA1_start_dt) ~ NA_character_,
      HA1_start_dt >= (MV_start_dt - hours(12)) &
        (is.na(MV_resolution_dt) | HA1_start_dt <= MV_resolution_dt) ~ "Да",
      TRUE ~ "Нет"
    ),

    RRT_required = factor(RRT_required, levels = c("Да","Нет")),
    MV_required  = factor(MV_required,  levels = c("Да","Нет")),

    # ---------------- outcome -> binary for logit ----------------
    # 0 = "Умер", 1 = "Выписан из ОРИТ".
    # Все остальные значения оставляем NA (исключаются из модели),
    # чтобы не смешивать "переведён/продолжает лечение" с исходом.
    outcome_std = str_to_lower(str_squish(as.character(outcome))),
    # ---------------- outcome -> binary for logit (в этой версии: 1 = смерть, 0 = выписан/продолжает) ----------------
    outcome_logit = case_when(
      is.na(outcome_std) ~ NA_integer_,
      str_detect(outcome_std, "умер|смерт") ~ 1L,
      str_detect(outcome_std, "выпис") ~ 0L,
      str_detect(outcome_std, "продолж|наход") ~ 0L,  # "Продолжает находиться в ОРИТ" -> 0
      TRUE ~ NA_integer_
    ),
    outcome_logit = factor(outcome_logit, levels = c(0, 1)))

# ---------------- верхняя часть  ----------------
vars_top <- c(
  "age_yr","sex","BMI","ICU_6_month_history","from_other_ICU_transfer",
  "icu_to_HA1_hours",
  "vasopressors_used","RRT_required","MV_required",
  "charlson_index",
  "pat_bacteremia","pat_gram_negative","pat_gram_positive","pat_fungal",
  "CT_count_pos","LPS_count_pos","devices_total_pos",
  "HA_efficency_CGI_E","outcome"
)

lbl_top <- list(
  age_yr = ru_or("age_yr"),
  sex = ru_or("sex"),
  BMI = ru_or("BMI"),
  ICU_6_month_history = ru_or("ICU_6_month_history"),
  from_other_ICU_transfer = ru_or("from_other_ICU_transfer"),
  icu_to_HA1_hours = "Время между поступлением в ОРИТ и началом ГС, ч",
  vasopressors_used = "Потребность в вазопрессорах",
  RRT_required = "Потребность в ЗПТ",
  MV_required  = "Потребность в ИВЛ",
  charlson_index = ru_or("charlson_index"),
  pat_bacteremia    = ru_or("pat_bacteremia"),
  pat_gram_negative = ru_or("pat_gram_negative"),
  pat_gram_positive = ru_or("pat_gram_positive"),
  pat_fungal        = ru_or("pat_fungal"),
  CT_count_pos = "Устройств CT на пациента, n",
  LPS_count_pos = "Устройств LPS на пациента, n",
  devices_total_pos = "Всего устройств на пациента, n",
  HA_efficency_CGI_E = ru_or("HA_efficency_CGI_E"),
  outcome = ru_or("outcome")
)

type_top  <- list(
  vasopressors_used ~ "dichotomous",
  RRT_required ~ "dichotomous",
  MV_required ~ "dichotomous"
)

value_top <- list(
  vasopressors_used ~ "Да",
  RRT_required ~ "Да",
  MV_required ~ "Да"
)

if (isTRUE(show_antibiotics) && "antibiotics_started" %in% names(pt_top)) {
  vars_top <- c(vars_top, "antibiotics_started")
  lbl_top$antibiotics_started <- "Антибиотики начаты (дата заполнена)"
  type_top  <- c(type_top,  list(antibiotics_started ~ "dichotomous"))
  value_top <- c(value_top, list(antibiotics_started ~ "Да"))
}

lbl_top <- lbl_top[names(lbl_top) %in% vars_top]


stat_top <- list(
  all_continuous() ~ "{median} ({p25}, {p75})",
  all_categorical() ~ "{p}% ({n})"
)

tbl_top <- pt_top %>%
  select(any_of(vars_top)) %>%
  tbl_summary(
    type = type_top,
    value = value_top,
    statistic = stat_top,
    label = lbl_top,
    missing = "no"
  )

# ------------ показания к гемосорбции (diag_wide уже загружен в ETL) ------------
tbl_indications <- NULL
diag_n <- NA_integer_

if (!is.null(diag_wide) && "pat_record_id" %in% names(diag_wide)) {
  diag_wide2 <- diag_wide %>%
    mutate(pat_record_id = as.character(pat_record_id)) %>%
    semi_join(pt_top %>% select(pat_record_id), by = "pat_record_id")

  diag_n <- nrow(diag_wide2)
  diag_cols <- setdiff(names(diag_wide2), "pat_record_id")

  count_yes <- function(v) {
    v <- as.character(v)
    v <- str_to_lower(str_squish(v))
    sum(v %in% c("1","да","yes","true"), na.rm = TRUE)
  }

  if (diag_n > 0 && length(diag_cols) > 0) {
    dx_order <- diag_wide2 %>%
      summarise(across(all_of(diag_cols), count_yes)) %>%
      pivot_longer(everything(), names_to = "dx", values_to = "n_yes") %>%
      arrange(desc(n_yes), dx) %>%
      pull(dx)

    diag_tbl <- diag_wide2 %>%
      mutate(across(all_of(dx_order), ~ factor(
        if_else(as.character(.) %in% c("1","Да","да","TRUE","true"), "Да", "Нет"),
        levels = c("Да","Нет")
      ))) %>%
      select(all_of(dx_order))

    tbl_indications <- diag_tbl %>%
      tbl_summary(
        type = everything() ~ "dichotomous",
        value = everything() ~ "Да",
        statistic = all_categorical() ~ "{p}% ({n})",
        label = rlang::set_names(as.list(dx_order), dx_order),
        missing = "no"
      )
  }
}

# ---------------- нижняя часть (колонки по timepoint) ----------------
pt_bottom <- patients_tidy %>%
  filter(timepoint %in% tps_bottom) %>%
  mutate(timepoint = factor(timepoint, levels = tps_bottom)) %>%
  distinct(pat_record_id, timepoint, .keep_all = TRUE)

u_lactate <- unit_mode(pt_bottom$lactate_unit)
u_creat   <- unit_mode(pt_bottom$creatinine_unit)
u_albumin <- unit_mode(pt_bottom$albumin_unit)
u_crp     <- unit_mode(pt_bottom$C_react_protein_unit)
u_pct     <- unit_mode(pt_bottom$procalcitonin_unit)
u_ddimer  <- unit_mode(pt_bottom$D_dimer_unit)
u_fibr    <- unit_mode(pt_bottom$fibrinogen_unit)

vars_bottom <- c(
  "SOFA","VIS2020","PaFiO2_calc",
  "lactate","creatinine","albumin","C_react_protein",
  "procalcitonin","D_dimer","fibrinogen",
  "leucocytes","lymphocytes","neutrophils","NLR",
  "thrombocytes"
)

lbl_bottom <- list(
  SOFA = ru_or("SOFA"),
  VIS2020 = ru_or("VIS2020"),
  PaFiO2_calc = paste0(ru_or("PaFiO2"), " (расч. по SpFiO2)"),
  lactate = lab_u(ru_or("lactate"), u_lactate),
  creatinine = lab_u(ru_or("creatinine"), u_creat),
  albumin = lab_u(ru_or("albumin"), u_albumin),
  C_react_protein = lab_u(ru_or("C_react_protein"), u_crp),
  procalcitonin = lab_u(ru_or("procalcitonin"), u_pct),
  D_dimer = lab_u(ru_or("D_dimer"), u_ddimer),
  fibrinogen = lab_u(ru_or("fibrinogen"), u_fibr),
  leucocytes = ru_or("leucocytes"),
  lymphocytes = ru_or("lymphocytes"),
  neutrophils = ru_or("neutrophils"),
  NLR = "NLR (нейтрофилы/лимфоциты)",
  thrombocytes = ru_or("thrombocytes")
)
lbl_bottom <- lbl_bottom[names(lbl_bottom) %in% vars_bottom]

tbl_bottom <- pt_bottom %>%
  select(timepoint, any_of(vars_bottom)) %>%
  tbl_summary(
    by = timepoint,
    statistic = list(all_continuous() ~ "{median} ({p25}, {p75})"),
    digits = list(NLR ~ 2),
    label = lbl_bottom,
    missing = "no"
  )

# ---------------- итог: верх (+показания) + низ ----------------
tbls <- list(tbl_top)
hdrs <- c("Общие характеристики")

if (!is.null(tbl_indications)) {
  tbls <- c(tbls, list(tbl_indications))
  hdrs <- c(hdrs, sprintf("Показания к гемосорбции (n=%d)", diag_n))
}

tbls <- c(tbls, list(tbl_bottom))
hdrs <- c(hdrs, "Параметры по временным точкам")

tbl_pat <- tbl_stack(tbls, group_header = hdrs) %>%
  modify_table_styling(rows = row_type == "label", columns = label, text_format = "bold") %>%
  modify_table_styling(rows = row_type %in% c("group_header", "group"), columns = label, text_format = "bold") %>%
  modify_caption("**2Reg: сводная таблица по пациентам**") %>%
  modify_header(label = "**Параметр**")

tbl_pat
```

# Корреляции (m12h_0h)

```{r}

#| label: kendall_corr_m12h_0h
#| cache: false
#| echo: false

# Смотрим попарные связи между количественными переменными на baseline.
# Выбран коэффициент Кенделла, потому что он устойчивее для небольших выборок
# и не требует нормального распределения.
vars_corr <- c(
  "age_yr","BMI","SOFA","icu_to_HA1_hours","VIS2020","PaFiO2_calc",
  "lactate","creatinine","albumin","C_react_protein","procalcitonin",
  "D_dimer","fibrinogen","leucocytes","lymphocytes","neutrophils","NLR",
  "thrombocytes"
)

pt_corr <- patients_tidy %>%
  filter(timepoint == "m12h_0h") %>%
  distinct(pat_record_id, .keep_all = TRUE) %>%
  select(any_of(vars_corr)) %>%
  mutate(across(everything(), ~ suppressWarnings(as.numeric(.))))

# Убираем переменные, где <2 наблюдений (иначе cor() даст NA по всей строке/колонке)
keep_vars <- names(pt_corr)[purrr::map_lgl(pt_corr, ~ sum(!is.na(.x)) >= 2)]
pt_corr <- pt_corr %>% select(all_of(keep_vars))


if (ncol(pt_corr) >= 2) {
  cor_mat <- cor(pt_corr, method = "kendall", use = "pairwise.complete.obs")

  # Матрица корреляций (округление для вывода)
  print(round(cor_mat, 2))

  # Кореллограмма (heatmap)
cor_mat <- cor(pt_corr, method = "kendall", use = "pairwise.complete.obs")

# оставляем нижний треугольник + диагональ
cor_mat_tri <- cor_mat
cor_mat_tri[upper.tri(cor_mat_tri, diag = FALSE)] <- NA

# Печать матрицы (будет треугольником)
print(round(cor_mat_tri, 2))

# Таблица для ggplot
cor_tbl <- as.data.frame(as.table(cor_mat_tri)) %>%
  rename(var1 = Var1, var2 = Var2, tau = Freq) %>%
  mutate(
    var1 = factor(var1, levels = colnames(cor_mat_tri)),
    var2 = factor(var2, levels = colnames(cor_mat_tri))
  ) %>%
  filter(!is.na(tau))

ggplot(cor_tbl, aes(var1, var2, fill = tau)) +
  geom_tile(color = "white", linewidth = 0.3) +
  geom_text(aes(label = sprintf("%.2f", tau)), size = 3) +
  scale_fill_gradient2(limits = c(-1, 1)) +
  coord_fixed() +
  theme_minimal(base_size = 11) +
  theme(
    axis.title = element_blank(),
    axis.text.x = element_text(angle = 45, hjust = 1, vjust = 1),
    panel.grid = element_blank()
  ) +
  labs(
    title = "Kendall τ, m12h_0h",
    fill = "Kendall τ"
  )
} else {
  cat("Недостаточно данных для расчёта корреляций (нужно ≥2 переменных с ≥2 наблюдениями).\n")
}

```

## Проверка PaFiO2 и фибриноген

```{r}

library(dplyr)
library(readr)
library(ggplot2)

dbg <- patients_tidy %>%
  filter(timepoint == "m12h_0h") %>%
  distinct(pat_record_id, .keep_all = TRUE) %>%
  transmute(
    pat_record_id,
    PaFiO2 = readr::parse_number(as.character(PaFiO2), locale = locale(decimal_mark = ",")),
    fibrinogen = readr::parse_number(as.character(fibrinogen), locale = locale(decimal_mark = ","))
  )

# 1) Сколько реально пар попало в расчёт
dbg2 <- dbg %>% filter(!is.na(PaFiO2), !is.na(fibrinogen))
n_pair <- nrow(dbg2)
cat("n_pair =", n_pair, "\n")

dbg2 %>% arrange(PaFiO2) %>% print(n = Inf)

# 2) Сколько уникальных значений (ties)
cat("unique PaFiO2 =", n_distinct(dbg2$PaFiO2), "\n")
cat("unique fibrinogen =", n_distinct(dbg2$fibrinogen), "\n")

# 3) Посмотреть сами пары (лучше сразу видно, почему τ=1)
print(dbg2 %>% arrange(PaFiO2))

# 4) Пересчитать тестом (он покажет оценку и p-value, и использованное n)
print(cor.test(dbg2$PaFiO2, dbg2$fibrinogen, method = "kendall", exact = FALSE))

# 5) Визуальная проверка
ggplot(dbg2, aes(PaFiO2, fibrinogen)) +
  geom_point() +
  theme_minimal()

# tmp <- patients_tidy %>%
#   filter(timepoint == "m12h_0h") %>%
#   distinct(pat_record_id, .keep_all = TRUE) %>%
#   transmute(
#     pat_record_id,
#     PaFiO2_raw = PaFiO2,
#     fibrinogen_raw = fibrinogen,
#     PaFiO2_num = readr::parse_number(as.character(PaFiO2_raw), locale = readr::locale(decimal_mark = ",")),
#     fibrinogen_num = readr::parse_number(as.character(fibrinogen_raw), locale = readr::locale(decimal_mark = ","))
#   )
# 
# cat("PaFiO2 non-NA raw:", sum(!is.na(tmp$PaFiO2_raw)), " numeric:", sum(!is.na(tmp$PaFiO2_num)), "\n")
# cat("fibrinogen non-NA raw:", sum(!is.na(tmp$fibrinogen_raw)), " numeric:", sum(!is.na(tmp$fibrinogen_num)), "\n")
# 
# tmp %>% filter(is.na(PaFiO2_num) & !is.na(PaFiO2_raw)) %>% head(20)
# tmp %>% filter(is.na(fibrinogen_num) & !is.na(fibrinogen_raw)) %>% head(20)
```

# VIS gtsummary

```{r}
#| label: gtsummary_vis
#| echo: true

# Отдельная компактная таблица только для VIS.
# Нужна, чтобы быстро показать оба варианта показателя:
# исходный VIS2020 и аналитический VIS2020_0_excl.
# Все переменные уже рассчитаны в ETL и лежат в patients_tidy:
#   VIS2020        — «сырой» VIS
#   VIS2020_0_excl — VIS для анализа (исключены пациенты с 0/NA на m12h_0h)

vis_df <- patients_tidy %>%
  select(timepoint, any_of(c("VIS2020", "VIS2020_0_excl")))

tbl_vis <- vis_df %>%
  tbl_summary(
    by = timepoint,
    type = list(all_continuous() ~ "continuous"),
    statistic = list(all_continuous() ~ "{median} ({p25}, {p75})"),
    label = list(
      VIS2020 ~ "VIS2020",
      VIS2020_0_excl ~ "VIS2020_0_excl"
    ),
    missing = "no"
  )

tbl_vis
```

# Logit A

```{r}

#| label: logit A
#| cache: true
#| echo: false

# Для логистической регрессии используем только baseline-строки
# и только тех пациентов, у кого исход можно интерпретировать как бинарный.
# Это защищает модель от смешивания разных клинических статусов.
df_base <- pt_top %>%
  filter(!is.na(outcome_logit))

# =========================================================
# Logit A: + first_sorption_type (factor)
# =========================================================
df_logit_type <- df_base %>%
  mutate(
    first_sorption_type = factor(first_sorption_type, levels = c("CT", "LPS"))
  ) %>%
  filter(!is.na(first_sorption_type)) %>%
  filter(!is.na(icu_to_HA1_hours), !is.na(SOFA), !is.na(age_yr))

model_logit_type <- glm(
  outcome_logit ~ I(icu_to_HA1_hours / 5) + SOFA + age_yr + first_sorption_type,
  data = df_logit_type,
  family = binomial(link = "logit")
)

tbl_regression(
  model_logit_type,
  exponentiate = TRUE,
  label = list(
    `I(icu_to_HA1_hours/5)` ~ "Время между поступлением в ОРИТ и началом ГС (на 5 ч)",
    SOFA ~ "SOFA",
    age_yr ~ "Возраст (полных лет)",
    first_sorption_type ~ "Тип первой сорбции (Гемоперфузия 1)"
  )
) %>%
  bold_labels()

# Прогноз вероятности смерти 
type_ref <- names(sort(table(df_logit_type$first_sorption_type), decreasing = TRUE))[1]

p_type <- tibble(
  icu_to_HA1_hours = c(0, 1, 3, 5),
  SOFA = median(df_logit_type$SOFA, na.rm = TRUE),
  age_yr = median(df_logit_type$age_yr, na.rm = TRUE),
  first_sorption_type = factor(type_ref, levels = levels(df_logit_type$first_sorption_type))
) %>%
  mutate(
    p_death = predict(model_logit_type, newdata = ., type = "response"),
    p_not_death = 1 - p_death
  )

p_type
summary(model_logit_type)




```

# Logit B

```{r}

#| label: logit B
#| cache: true
#| echo: false

# Для логистической регрессии используем только baseline-строки
# и только тех пациентов, у кого исход можно интерпретировать как бинарный.
# Это защищает модель от смешивания разных клинических статусов.
df_base <- pt_top %>%
  filter(!is.na(outcome_logit))

# =========================================================
# Logit B: + ICU_to_first_LPS (continuous, на 5 часов)
# =========================================================
df_logit_lps <- df_base %>%
  filter(!is.na(ICU_to_first_LPS)) %>%                 # если вдруг есть NA — чтобы не терять строки "молча"
  filter(!is.na(icu_to_HA1_hours), !is.na(SOFA), !is.na(age_yr))

model_logit_lps <- glm(
  outcome_logit ~ SOFA + age_yr + I(ICU_to_first_LPS / 5),
  data = df_logit_lps,
  family = binomial(link = "logit")
)

tbl_regression(
  model_logit_lps,
  exponentiate = TRUE,
  label = list(
  
    SOFA ~ "SOFA",
    age_yr ~ "Возраст (полных лет)",
    `I(ICU_to_first_LPS/5)` ~ "Время от ICU до первого LPS (на 5 ч)"
  )
) %>%
  bold_labels()

# Прогноз вероятности смерти
p_lps <- tibble(
  icu_to_HA1_hours = c(0, 1, 3, 5),
  SOFA = median(df_logit_lps$SOFA, na.rm = TRUE),
  age_yr = median(df_logit_lps$age_yr, na.rm = TRUE),
  ICU_to_first_LPS = median(df_logit_lps$ICU_to_first_LPS, na.rm = TRUE)
) %>%
  mutate(
    p_death = predict(model_logit_lps, newdata = ., type = "response"),
    p_not_death = 1 - p_death
  )

p_lps
summary(model_logit_lps)
```

# Сорбции gtsummary

```{r}

#| label: ha_gtsummary_compact
#| cache: true
#| echo: false

# Небольшая вспомогательная функция:
# если x не пустой и не NA, возвращаем x, иначе y.
`%||%` <- function(x, y) {
  if (!is.null(x) && length(x) && !is.na(x) && as.character(x) != "") x else y
}

sq <- function(x) str_squish(as.character(x))
norm0 <- function(x){
  x <- str_to_lower(sq(x))
  x <- str_replace_all(x, "\u00A0", " ")
  str_squish(x)
}

# используем dict_ha, который уже создан в ETL (без повторного read_excel)
ha_label_map <- tibble::deframe(dict_ha)  # names = short, values = ru
label_list <- rlang::set_names(as.list(ha_label_map), names(ha_label_map))

# Готовим таблицу процедур для summary:
# нормализуем антикоагуляцию, определяем наличие ЗПТ и делаем бинарный признак
# "нежелательные явления указаны / не указаны".
ha_proc <- HA_tidy %>%
  mutate(
    HA_anticoagulation_cat = case_when(
      HA_anticoagulation == "Цитрат"  ~ "Цитрат",
      HA_anticoagulation == "Гепарин" ~ "Гепарин",
      HA_anticoagulation == "Другой" &
        norm0(coalesce(HA_anticoagulation_other, "")) %in% c("без антикоагуляции") ~ "Без антикоагуляции",
      HA_anticoagulation == "Другой" ~ "Другой",
      TRUE ~ "Другой"
    ),

    other_norm = norm0(coalesce(HA_other_methods, "")),
    has_rrt  = str_detect(other_norm, "гемодиафильтрац|гемодиализ|гемофильтрац"),

    rrt_grp = if_else(has_rrt, "В комбинации с ЗПТ", "Изолированная гемосорбция") %>%
      factor(levels = c("В комбинации с ЗПТ", "Изолированная гемосорбция")),

    adverse_effects_filled = if_else({
      x <- norm0(HA_adverse_effects)
      !(is.na(x) | x %in% c("", "нет", "не было", "-", "—"))
    }, "Да", "Нет")
  ) %>%
  select(-other_norm, -has_rrt)

ha_tbl_data <- ha_proc %>%
  select(
    rrt_grp,
    HA_cartrige_type,
    HA_duration,
    HA_avg_blood_flow,
    HA_anticoagulation_cat,
    adverse_effects_filled
  )

# N в шапке столбцов (как в gtsummary_pat)
n_all <- nrow(ha_tbl_data)
n_rrt <- sum(ha_tbl_data$rrt_grp == "В комбинации с ЗПТ", na.rm = TRUE)
n_iso <- sum(ha_tbl_data$rrt_grp == "Изолированная гемосорбция", na.rm = TRUE)

tbl <- ha_tbl_data %>%
  tbl_summary(
    by = rrt_grp,
    statistic = list(
      all_continuous() ~ "{median} ({p25}, {p75})",
      all_categorical() ~ "{p}% ({n})"
    ),
    missing = "no",
    sort = all_categorical() ~ "frequency",
    label = c(
      label_list,
      list(
        HA_anticoagulation_cat = ha_label_map[["HA_anticoagulation"]] %||% "Антикоагуляция",
        adverse_effects_filled = "Нежелательные явления: указано"
      )
    )
  ) %>%
  add_overall(last = FALSE) %>%
  modify_header(
    label ~ "**Параметр**",
    stat_0 ~ paste0("**All (N=", n_all, ")**"),
    stat_1 ~ paste0("**В комбинации с ЗПТ (N=", n_rrt, ")**"),
    stat_2 ~ paste0("**Изолированная гемосорбция (N=", n_iso, ")**")
  ) %>%
  modify_caption("**Сводная таблица по сорбциям**") %>%
  bold_labels()

tbl_ha_gtsummary_compact <- tbl

tbl_ha_gtsummary_compact
```

# Проверка дат по точкам

```{r}
#| label: check_clin_assess_vs_first_sorption
#| cache: true
#| echo: false

library(dplyr)
library(tidyr)
library(openxlsx)

check_clin_assess_dates <- patients_tidy %>%
  filter(timepoint %in% c("m12h_0h", "48h_72h")) %>%
  select(
    pat_record_id,
    timepoint,
    clin_assess_dt,
    first_sorption_dt,
    SOFA
  ) %>%
  distinct(pat_record_id, timepoint, .keep_all = TRUE) %>%
  pivot_wider(
    names_from = timepoint,
    values_from = c(clin_assess_dt, SOFA),
    names_glue = "{.value}_{timepoint}"
  ) %>%
  mutate(
    diff_m12_vs_first_sorp_hours = as.numeric(
      difftime(clin_assess_dt_m12h_0h, first_sorption_dt, units = "hours")
    ),
    diff_48h_vs_m12_hours = as.numeric(
      difftime(clin_assess_dt_48h_72h, clin_assess_dt_m12h_0h, units = "hours")
    ),
    diff_m12_vs_first_sorp_days = diff_m12_vs_first_sorp_hours / 24,
    diff_48h_vs_m12_days        = diff_48h_vs_m12_hours / 24
  ) %>%
  select(
    pat_record_id,
    first_sorption_dt,
    clin_assess_dt_m12h_0h,
    SOFA_m12h_0h,
    diff_m12_vs_first_sorp_hours,
    diff_m12_vs_first_sorp_days,
    clin_assess_dt_48h_72h,
    SOFA_48h_72h,
    diff_48h_vs_m12_hours,
    diff_48h_vs_m12_days
  ) %>%
  arrange(pat_record_id)

# вывод таблицы в документ
check_clin_assess_dates

# сохранение в Excel
openxlsx::write.xlsx(
  check_clin_assess_dates,
  file = "check_clin_assess_dates.xlsx",
  overwrite = TRUE
)
```

# ВЫБОРКА: ВРЕМЕННЫЕ ТОЧКИ ПО СОРБЦИИ

```{r}

#| label: analysis_population_by_clin_assess_order
#| cache: true
#| echo: false

library(dplyr)

# Порядок новых аналитических точек
tp_keep <- c("m12h_0h", "48h_72h")

# Создаем новый датасет, не меняя patients_tidy:
# - m12h_0h = последняя клиническая оценка до или ровно в момент first_sorption_dt
# - 48h_72h = первая клиническая оценка строго после first_sorption_dt
#


patients_before <- patients_tidy %>%
  filter(!is.na(first_sorption_dt), !is.na(clin_assess_dt)) %>%
  group_by(pat_record_id) %>%
  filter(clin_assess_dt <= first_sorption_dt) %>%
  slice_max(clin_assess_dt, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  mutate(
    timepoint_old = as.character(timepoint),
    timepoint = factor("m12h_0h", levels = tp_keep, ordered = TRUE),
    clin_to_first_h = round(as.numeric(difftime(clin_assess_dt, first_sorption_dt, units = "hours")), 1),
    selection_rule = "last clin_assess_dt <= first_sorption_dt"
  )

patients_after <- patients_tidy %>%
  filter(!is.na(first_sorption_dt), !is.na(clin_assess_dt)) %>%
  group_by(pat_record_id) %>%
  filter(clin_assess_dt > first_sorption_dt) %>%
  slice_min(clin_assess_dt, n = 1, with_ties = FALSE) %>%
  ungroup() %>%
  mutate(
    timepoint_old = as.character(timepoint),
    timepoint = factor("48h_72h", levels = tp_keep, ordered = TRUE),
    clin_to_first_h = round(as.numeric(difftime(clin_assess_dt, first_sorption_dt, units = "hours")), 1),
    selection_rule = "first clin_assess_dt > first_sorption_dt"
  )

# Объединяем обе выбранные строки в один датасет, считаем разницу дат клин. оценок
# пересчёт VIS2020_0_excl
patients_analysis_clin_window <- bind_rows(patients_before, patients_after) %>%
  arrange(pat_record_id, timepoint) %>%
  group_by(pat_record_id) %>%
  mutate(
    VIS2020_m12_new = dplyr::first(VIS2020[timepoint == "m12h_0h"]),
    VIS2020_0_excl = dplyr::if_else(
      is.na(VIS2020_m12_new) | VIS2020_m12_new <= 0,
      NA_real_,
      as.numeric(VIS2020)
    )
  ) %>%
    mutate(
    clin_assess_dt_m12h_0h = dplyr::first(clin_assess_dt[timepoint == "m12h_0h"]),
    clin_assess_dt_48h_72h = dplyr::first(clin_assess_dt[timepoint == "48h_72h"]),
    clin_assess_diff_h = as.numeric(
      difftime(clin_assess_dt_48h_72h, clin_assess_dt_m12h_0h, units = "hours")
    )
  ) %>%
  ungroup() %>%
  select(-VIS2020_m12_new)

# Короткая проверка: сколько строк получилось по новым точкам
patients_analysis_clin_window %>%
  count(timepoint, name = "n_rows")

# сохранение в Excel
openxlsx::write.xlsx(
  patients_analysis_clin_window,
  file = "patients_analysis_clin_window.xlsx",
  overwrite = TRUE
)
```

# Парные сравнения (-12-0) - (48-72)ч

## Wilcox по выборке

```{r}

#| label: paired_test_m12h_0h_48h_72h
#| cache: true
#| echo: false

library(dplyr)
library(tidyr)
library(gtsummary)
library(tibble)
library(broom)

# Предполагается, что выше уже создан patients_analysis_clin_window
tp_keep <- c("m12h_0h", "48h_72h")

# Переменные для анализа
vars <- c(
  "SOFA","PaFiO2_calc","lactate","creatinine","albumin","C_react_protein",
  "procalcitonin","D_dimer","fibrinogen","leucocytes","lymphocytes",
  "neutrophils","NLR","thrombocytes","VIS2020", "VIS2020_0_excl"
)
vars <- intersect(vars, names(patients_analysis_clin_window))

# Берем только LPS и только реальные пары
patients_paired <- patients_analysis_clin_window %>%
  filter(first_sorption_type == "LPS") %>%
  select(pat_record_id, timepoint, clin_assess_dt, any_of(vars)) %>%
  distinct(pat_record_id, timepoint, .keep_all = TRUE) %>%
  group_by(pat_record_id) %>%
  filter(n_distinct(timepoint) == 2) %>%
  ungroup()

# Средняя разница clin_assess_dt между точками m12h_0h и 48h_72h
mean_clin_assess_diff_h <- patients_paired %>%
  select(pat_record_id, timepoint, clin_assess_dt) %>%
  pivot_wider(
    names_from = timepoint,
    values_from = clin_assess_dt
  ) %>%
  mutate(
    clin_assess_diff_h = as.numeric(
      difftime(`48h_72h`, m12h_0h, units = "hours")
    )
  ) %>%
  summarise(mean_clin_assess_diff_h = mean(clin_assess_diff_h, na.rm = TRUE)) %>%
  pull(mean_clin_assess_diff_h)

mean_clin_assess_diff_h_txt <- ifelse(
  is.na(mean_clin_assess_diff_h),
  "NA",
  sprintf("%.1f", mean_clin_assess_diff_h)
)

# Парный Wilcoxon: разность интерпретируем как 48h_72h - m12h_0h
paired_wilcox_48_minus_0 <- function(data, variable, by, group, conf.level = 0.95, ...) {
  df <- data %>%
    select(all_of(c(group, by, variable))) %>%
    filter(
      !is.na(.data[[group]]),
      !is.na(.data[[by]])
    ) %>%
    distinct(.data[[group]], .data[[by]], .keep_all = TRUE) %>%
    pivot_wider(
      id_cols = all_of(group),
      names_from = all_of(by),
      values_from = all_of(variable)
    ) %>%
    filter(!is.na(.data[["m12h_0h"]]) & !is.na(.data[["48h_72h"]]))

  if (nrow(df) < 2) {
    return(
      tibble(
        estimate = NA_real_,
        conf.low = NA_real_,
        conf.high = NA_real_,
        p.value = NA_real_
      )
    )
  }

  wt <- tryCatch(
    suppressWarnings(
      stats::wilcox.test(
        x = df[["48h_72h"]],
        y = df[["m12h_0h"]],
        paired = TRUE,
        conf.int = TRUE,
        conf.level = conf.level,
        exact = FALSE
      )
    ),
    error = function(e) NULL
  )

  if (is.null(wt)) {
    return(
      tibble(
        estimate = NA_real_,
        conf.low = NA_real_,
        conf.high = NA_real_,
        p.value = NA_real_
      )
    )
  }

  broom::tidy(wt) %>%
    transmute(
      estimate = .data$estimate,
      conf.low = .data$conf.low,
      conf.high = .data$conf.high,
      p.value = .data$p.value
    )
}

tbl <-
  patients_paired %>%
  tbl_summary(
    by = timepoint,
    include = -c(pat_record_id, clin_assess_dt),
    statistic = all_continuous() ~ "{median} ({p25}, {p75})",
    missing = "no"
  ) %>%
  add_difference(
    test  = all_continuous() ~ paired_wilcox_48_minus_0,
    group = pat_record_id
  ) %>%
  modify_fmt_fun(
    estimate ~ function(x) ifelse(is.na(x), NA_character_, sprintf("%+.2f", x))
  ) %>%
  modify_header(estimate ~ "**Difference**") %>%
  modify_footnote(
    estimate ~ paste0(
      "Paired Wilcoxon estimate for -12-0h и 48-72h ",
      "Средняя разница между клиническими оценками в точках -12-0 ч и 48-72 
      ч: ",
      mean_clin_assess_diff_h_txt, " ч"
    )
  ) %>%
  modify_caption("Парные сравнения -12-0 и 48-72 часов (только LPS)") %>%
  bold_labels()

tbl

# PowerPoint
tbl %>%
  as_flex_table() %>%
  flextable::save_as_pptx(path = "paired_test.pptx")
```

## SOFA Scatter по выборке

```{r}

#| label: scatter_SOFA_m12h_0h_vs_48h_72h
#| cache: true
#| warning: false
#| message: false
#| fig-width: 6
#| fig-height: 6

library(dplyr)
library(tidyr)
library(ggplot2)

sofa_scatter <- patients_analysis_clin_window %>%
  select(pat_record_id, timepoint, SOFA, clin_assess_dt, ICU_in_dt) %>%
  distinct(pat_record_id, timepoint, .keep_all = TRUE) %>%
  pivot_wider(
    names_from = timepoint,
    values_from = c(SOFA, clin_assess_dt),
    names_glue = "{.value}_{timepoint}"
  ) %>%
  mutate(
    hours_from_icu_to_clin_m12 = as.numeric(
      difftime(clin_assess_dt_m12h_0h, ICU_in_dt, units = "hours")
    )
  ) %>%
  filter(
    !is.na(SOFA_m12h_0h),
    !is.na(SOFA_48h_72h),
    !is.na(hours_from_icu_to_clin_m12)
  )

n_points <- nrow(sofa_scatter)

lims <- range(
  c(sofa_scatter$SOFA_m12h_0h, sofa_scatter$SOFA_48h_72h),
  na.rm = TRUE
)

ggplot(
  sofa_scatter,
  aes(
    x = SOFA_m12h_0h,
    y = SOFA_48h_72h
  )
) +
  geom_jitter(
    aes(color = hours_from_icu_to_clin_m12),
    width = 0.15,
    height = 0.15,
    alpha = 0.8,
    size = 2
  ) +
  geom_smooth(se = TRUE) +
  geom_abline(intercept = 0, slope = 1, linetype = "dashed") +
  scale_x_continuous(
    breaks = seq(floor(lims[1]), ceiling(lims[2]), by = 1)
  ) +
  scale_y_continuous(
    breaks = seq(floor(lims[1]), ceiling(lims[2]), by = 1)
  ) +
  coord_equal(xlim = lims, ylim = lims) +
  annotate(
    "text",
    x = Inf, y = -Inf,
    label = paste0("n = ", n_points),
    hjust = 1.1, vjust = -0.8,
    size = 4
  ) +
  labs(
    x = "SOFA, -12–0 часов",
    y = "SOFA, 48–72 часа",
    color = "Часы от ICU_in_dt\nдо clin_assess_dt\nв точке -12–0 ч"
  ) +
  theme_bw()

```

# Графики

```{r}

#| label: plots_m12h_0h_48h_72h
#| cache: true
#| warning: false
#| message: false
#| fig-width: 7
#| fig-height: 4

library(dplyr)
library(tidyr)
library(purrr)
library(ggplot2)
library(stringr)

# Ниже строим отдельный график для каждой переменной:
# коробка с квартилями + индивидуальные линии пациента + p-value парного теста.

# Ожидается, что vars, tp_keep и patients_paired уже существуют

# ---- подписи timepoint для оси X ----
tp_labels <- c(
  m12h_0h  = "-12-0 часов",
  `48h_72h` = "48-72 часа"
)

# ---- short -> ru из словаря ----
var_label_map <- dict_pat %>%
  distinct(short, ru) %>%
  filter(!is.na(short), short != "", !is.na(ru), ru != "") %>%
  { setNames(.$ru, .$short) }

lab_var <- function(v) {
  v <- as.character(v)
  lbl <- unname(var_label_map[v])  
  if (is.na(lbl) || lbl == "") v else lbl
}

# ---- переменные ----
vars_plot <- vars

# Логарифмируем ось Y только для выбранных переменных
vars_log_Y <- c("lactate", "D_dimer", "lymphocytes", "procalcitonin", "NLR", "VIS2020_0_excl")

plot_long <- patients_paired %>%
  select(pat_record_id, timepoint, any_of(vars_plot)) %>%
  pivot_longer(
    cols = -c(pat_record_id, timepoint),
    names_to = "variable",
    values_to = "value"
  ) %>%
  mutate(
    timepoint = factor(timepoint, levels = tp_keep),
    variable  = factor(variable, levels = vars_plot),
    value     = as.numeric(value),
    # для лог-переменных убираем 0 и отрицательные (иначе log10 -> -Inf)
    value = if_else(as.character(variable) %in% vars_log_Y & !is.na(value) & value <= 0,
                    NA_real_, value)
  )

# оставляем только парные наблюдения по каждой переменной (2 timepoint и оба значения не NA)
plot_long_paired <- plot_long %>%
  group_by(variable, pat_record_id) %>%
  filter(
    n_distinct(timepoint) == 2,
    sum(!is.na(value)) == 2
  ) %>%
  ungroup()

q <- function(x, p) {
  if (all(is.na(x))) NA_real_
  else as.numeric(quantile(x, p, na.rm = TRUE, names = FALSE))
}

# статистики считаем на парных значениях
stats_data <- plot_long_paired %>%
  group_by(variable, timepoint) %>%
  summarise(
    n   = sum(!is.na(value)),
    Me  = if (all(is.na(value))) NA_real_ else median(value, na.rm = TRUE),
    Q1  = q(value, 0.25),
    Q3  = q(value, 0.75),
    P05 = q(value, 0.05),
    P95 = q(value, 0.95),
    .groups = "drop"
  )

make_plot <- function(var_name) {
  df_stat <- stats_data %>% filter(variable == var_name)
  df_raw  <- plot_long_paired %>% filter(variable == var_name) %>% drop_na(value)

  df_line <- df_raw %>%
    group_by(pat_record_id) %>%
    filter(n_distinct(timepoint) == 2, n() == 2) %>%
    ungroup()

  # ---- p-value (paired t-test) ----
  w <- df_raw %>%
    select(pat_record_id, timepoint, value) %>%
    tidyr::pivot_wider(names_from = timepoint, values_from = value)

  p_txt <- "paired t-test p = NA (too few pairs)"
  if (nrow(w) >= 2 &&
      all(tp_keep %in% names(w)) &&
      sum(!is.na(w[[tp_keep[1]]]) & !is.na(w[[tp_keep[2]]])) >= 2) {
    pval <- stats::t.test(w[[tp_keep[1]]], w[[tp_keep[2]]], paired = TRUE)$p.value
    p_txt <- paste0("paired t-test p = ", format.pval(pval, digits = 2, eps = 1e-3))
  }

  p <- ggplot() +
    geom_boxplot(
      data = df_stat,
      aes(
        x = timepoint,
        ymin = P05, ymax = P95,
        lower = Q1, middle = Me, upper = Q3,
        fill = timepoint
      ),
      stat = "identity",
      outlier.shape = NA,
      alpha = 0.55
    ) +
    geom_line(
      data = df_line,
      aes(x = timepoint, y = value, group = pat_record_id),
      alpha = 0.25
    ) +
    geom_point(
      data = df_raw,
      aes(x = timepoint, y = value),
      position = position_jitter(width = 0.008, height = 0),
      size = 0.9,
      alpha = 0.35
    ) +
    scale_x_discrete(labels = tp_labels) +
    labs(title = lab_var(var_name), subtitle = p_txt, x = NULL, y = NULL) +
    theme_bw() +
    theme(legend.position = "none")

  # Лог-шкала только для выбранных переменных
  if (as.character(var_name) %in% vars_log_Y) {
    p <- p + scale_y_log10(expand = expansion(mult = c(0.1, 0.1)))
  }

  p
}

plots_list <- vars_plot %>%
  set_names() %>%
  purrr::map(make_plot)

purrr::walk(plots_list, print)
```

# Проверка VIS2020_0_excl

```{r}

#| label: pptx_slide1
#| include: false

vis_tbl_pairs <- patients_analysis_clin_window %>%
  select(pat_record_id, timepoint, VIS2020_0_excl) %>%
  distinct(pat_record_id, timepoint, .keep_all = TRUE) %>%
  pivot_wider(names_from = timepoint, values_from = VIS2020_0_excl) %>%
  filter(!is.na(m12h_0h), !is.na(`48h_72h`))

vis_plot_pairs <- patients_analysis_clin_window %>%
  select(pat_record_id, timepoint, VIS2020_0_excl) %>%
  mutate(value = if_else(!is.na(VIS2020_0_excl) & VIS2020_0_excl <= 0, NA_real_, VIS2020_0_excl)) %>%
  select(-VIS2020_0_excl) %>%
  distinct(pat_record_id, timepoint, .keep_all = TRUE) %>%
  pivot_wider(names_from = timepoint, values_from = value) %>%
  filter(!is.na(m12h_0h), !is.na(`48h_72h`))

nrow(vis_tbl_pairs)
nrow(vis_plot_pairs)

setdiff(vis_tbl_pairs$pat_record_id, vis_plot_pairs$pat_record_id)
```

# Презентация (PPTX)

## Слайд 1. Общие показатели

```{r}
#| label: pptx_slide1
#| include: true

stopifnot(exists("patients_tidy"), exists("HA_tidy"))

cartridge_col <- dplyr::case_when(
  "HA_cartrige_type" %in% names(HA_tidy) ~ "HA_cartrige_type",
  "HA_cartrige_type_std" %in% names(HA_tidy) ~ "HA_cartrige_type_std",
  TRUE ~ NA_character_
)
if (is.na(cartridge_col)) stop("Не найден столбец HA_cartrige_type / HA_cartrige_type_std в HA_tidy")

n_pat     <- dplyr::n_distinct(patients_tidy$pat_record_id)
n_centers <- dplyr::n_distinct(patients_tidy$organization)

n_ct  <- sum(HA_tidy[[cartridge_col]] == "CT",  na.rm = TRUE)
n_lps <- sum(HA_tidy[[cartridge_col]] == "LPS", na.rm = TRUE)
n_lps_neo <- sum(HA_tidy[[cartridge_col]] == "LPS NEO", na.rm = TRUE)

mean_sorp <- HA_tidy %>%
  filter(!is.na(pat_record_id), pat_record_id != "") %>%
  count(pat_record_id, name = "n_sorptions") %>%
  summarise(mean = mean(n_sorptions), .groups = "drop") %>%
  pull(mean)

# Период: min–max по HA_start_dt (если есть); иначе пробуем распарсить raw
dt_vec <- HA_tidy %>%
  filter(HA_parameter == "Гемоперфузия 1") %>%
  { 
    if ("HA_start_dt" %in% names(.)) .$HA_start_dt
    else if ("HA_start_datetime_raw" %in% names(.)) suppressWarnings(as.POSIXct(.$HA_start_datetime_raw, tz = "UTC"))
    else suppressWarnings(as.POSIXct(.$HA_start_datetime, tz = "UTC"))
  }
dt_vec <- dt_vec[!is.na(dt_vec)]

period_txt <- if (!length(dt_vec)) {
  "n/a"
} else {
  paste0(
    format(as.Date(min(dt_vec), tz = "UTC"), "%d.%m.%Y"),
    "–",
    format(as.Date(max(dt_vec), tz = "UTC"), "%d.%m.%Y")
  )
}

table1 <- tibble::tibble(
  Показатель = c("Период", "Пациентов", "Центров", "Сорбций CT", "Сорбций LPS", "Сорбций LPS NEO", "Сорбций на пациента в среднем"),
  Значение   = c(period_txt, fmt_int(n_pat), fmt_int(n_centers), fmt_int(n_ct), fmt_int(n_lps),fmt_int(n_lps_neo), fmt_2(mean_sorp))
)

flx1 <- flextable::flextable(table1) %>% flextable::autofit()
invisible(flextable::save_as_pptx(flx1, path = "table1_general.pptx"))
flx1
```

## Слайд 2. Данные по пациентам на момент включения

```{r}

#| label: pptx_slide2
#| include: true

# Блоки ниже готовят отдельные объекты для слайдов PowerPoint.
# stopifnot() нужен как защита: если нужная таблица не была создана выше,
# скрипт остановится с понятной ошибкой, а не продолжит молча работать с пустотой.
stopifnot(exists("patients_tidy"))

tp0 <- "m12h_0h"

pt0 <- patients_tidy %>%
  filter(timepoint == tp0) %>%
  distinct(pat_record_id, .keep_all = TRUE)

if (!"first_sorption_type" %in% names(pt0)) stop("В patients_tidy нет first_sorption_type (ожидается из ETL).")

# --- Основные характеристики ---
vars_base <- c(
  "first_sorption_type",
  "age_yr",
  "sex",
  "icu_to_HA1_hours",
  "SOFA",
  "VIS2020_0_excl",
  "PaFiO2_calc",
  "procalcitonin",
  "C_react_protein",
  "lymphocytes",
  "thrombocytes",
  "lactate"
)

tbl_base <- pt0 %>%
  select(any_of(vars_base)) %>%
  tbl_summary(
    by = first_sorption_type,
    type = list(
      age_yr ~ "continuous",
      icu_to_HA1_hours ~ "continuous",
      SOFA ~ "continuous",
      VIS2020_0_excl ~ "continuous",
      PaFiO2_calc ~ "continuous",
      procalcitonin ~ "continuous",
      C_react_protein ~ "continuous",
      lymphocytes ~ "continuous",
      thrombocytes ~ "continuous",
      lactate ~ "continuous",
      sex ~ "categorical"
    ),
    statistic = list(
      all_continuous() ~ "{median} ({p25}, {p75})",
      all_categorical() ~ "{p}% ({n})"
    ),
    label = list(
      age_yr = "Возраст (лет)",
      sex = "Пол",
      icu_to_HA1_hours = "Время между поступлением в ОРИТ и началом ГС, ч",
      SOFA = "SOFA",
      VIS2020_0_excl = "Индекс VIS2020",
      PaFiO2_calc = "Индекс оксигенации (PaO2/FiO2)",
      procalcitonin = "Прокальцитонин",
      C_react_protein = "C-реактивный белок",
      lymphocytes = "Лимфоциты",
      thrombocytes = "Тромбоциты",
      lactate = "Лактат"
    ),
    missing = "no"
  ) %>%
  modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
  bold_labels()

# --- Показания к гемосорбции: септические (1) vs асептические (0) ---
tbl_indications <- NULL
if ("is_septic" %in% names(pt0)) {
  pt0_septic <- pt0 %>%
    transmute(
      first_sorption_type,
      is_septic_cat = factor(
        dplyr::case_when(
          is_septic == 1 ~ "Септические",
          is_septic == 0 ~ "Асептические",
          TRUE ~ NA_character_
        ),
        levels = c("Септические", "Асептические")
      )
    )

  tbl_indications <- pt0_septic %>%
    tbl_summary(
      by = first_sorption_type,
      type = list(is_septic_cat ~ "categorical"),
      statistic = list(all_categorical() ~ "{n} ({p}%)"),
      label = list(is_septic_cat = "Септические / асептические"),
      missing = "no"
    ) %>%
    modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
    bold_labels()
}

# --- Патогены (грам+/грам-): на слайде оставляем только две категории ---
# "Положительный посев" и "Отрицательный посев".
# Категорию N/A здесь специально не показываем: она превращена в NA на этапе *_slide.
# Важно: если после такой перекодировки в подблоке не остается ни одной непустой переменной,
# tbl_summary() упадет на пустом наборе столбцов. Поэтому сначала проверяем,
# какие pathogen-переменные реально есть и содержат хотя бы одно непустое значение.
if (!"pat_gram_positive_slide" %in% names(pt0) && "pat_gram_positive" %in% names(pt0)) {
  pt0 <- pt0 %>%
    dplyr::mutate(
      pat_gram_positive_slide = factor(
        dplyr::case_when(
          norm_txt(pat_gram_positive) %in% c("Положительный посев", "Положительный результат") ~ "Положительный посев",
          norm_txt(pat_gram_positive) %in% c("Отрицательный посев", "Отрицательный результат") ~ "Отрицательный посев",
          TRUE ~ NA_character_
        ),
        levels = c("Положительный посев", "Отрицательный посев")
      )
    )
}
if (!"pat_gram_negative_slide" %in% names(pt0) && "pat_gram_negative" %in% names(pt0)) {
  pt0 <- pt0 %>%
    dplyr::mutate(
      pat_gram_negative_slide = factor(
        dplyr::case_when(
          norm_txt(pat_gram_negative) %in% c("Положительный посев", "Положительный результат") ~ "Положительный посев",
          norm_txt(pat_gram_negative) %in% c("Отрицательный посев", "Отрицательный результат") ~ "Отрицательный посев",
          TRUE ~ NA_character_
        ),
        levels = c("Положительный посев", "Отрицательный посев")
      )
    )
}

path_vars_present <- intersect(
  c("pat_gram_positive_slide", "pat_gram_negative_slide"),
  names(pt0)
)
path_vars_nonempty <- path_vars_present[
  vapply(path_vars_present, function(v) any(!is.na(pt0[[v]])), logical(1))
]

tbl_path <- NULL
if (length(path_vars_nonempty) > 0) {
  vars_path <- c("first_sorption_type", path_vars_nonempty)

  tbl_path <- pt0 %>%
    select(any_of(vars_path)) %>%
    tbl_summary(
      by = first_sorption_type,
      type = all_categorical() ~ "categorical",
      statistic = all_categorical() ~ "{n} ({p}%)",
      label = list(
        pat_gram_positive_slide = "Грамположительные",
        pat_gram_negative_slide = "Грамотрицательные"
      ),
      missing = "no"
    ) %>%
    modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
    bold_labels()
}

# --- Сборка в одну таблицу ---
tbl_list <- list(tbl_base)
hdrs <- c("Исходные по пациентам")
if (!is.null(tbl_indications)) {
  tbl_list <- append(tbl_list, list(tbl_indications))
  hdrs <- c(hdrs, "Показания к гемосорбции")
}
if (!is.null(tbl_path)) {
  tbl_list <- append(tbl_list, list(tbl_path))
  hdrs <- c(hdrs, "Патогены")
}

tbl2 <- gtsummary::tbl_stack(tbl_list, group_header = hdrs)

# Сделать один столбец "Characteristic": перенести group headers в него и убрать отдельный столбец Group
if (!is.null(tbl2$table_body) && "groupname" %in% names(tbl2$table_body)) {
  tbl2$table_body <- tbl2$table_body %>%
    dplyr::mutate(
      label = dplyr::if_else(.data$row_type == "group_header", .data$groupname, .data$label)
    ) %>%
    dplyr::select(-groupname)

  if (!is.null(tbl2$table_header) && "column" %in% names(tbl2$table_header)) {
    tbl2$table_header <- tbl2$table_header %>%
      dplyr::filter(.data$column != "groupname")
  }
}

flx2 <- gtsummary::as_flex_table(tbl2)
invisible(flextable::save_as_pptx(flx2, path = "table2_patients.pptx"))
flx2
```

### Слайд 2а

```{r}

#| label: pptx_slide2_lps_total
#| include: true

stopifnot(exists("patients_tidy"))

tp0 <- "m12h_0h"

pt0 <- patients_tidy %>%
  filter(timepoint == tp0) %>%
  distinct(pat_record_id, .keep_all = TRUE)

if (!"first_sorption_type" %in% names(pt0)) {
  stop("В patients_tidy нет first_sorption_type (ожидается из ETL).")
}

# Объединяем LPS и LPS NEO в одну группу LPS TOTAL
pt0 <- pt0 %>%
  mutate(
    first_sorption_type_total = dplyr::case_when(
      first_sorption_type %in% c("LPS", "LPS NEO") ~ "LPS TOTAL",
      TRUE ~ as.character(first_sorption_type)
    )
  )

# Чтобы LPS TOTAL шел первым столбцом
group_levels <- c(
  "LPS TOTAL",
  setdiff(sort(unique(pt0$first_sorption_type_total)), "LPS TOTAL")
)
group_levels <- unique(group_levels[group_levels %in% unique(pt0$first_sorption_type_total)])

pt0 <- pt0 %>%
  mutate(
    first_sorption_type_total = factor(first_sorption_type_total, levels = group_levels),

    # Потребность в вазопрессорах
    vasopressors_used = dplyr::case_when(
      stringr::str_to_lower(vasopressors_if_used) == "да"  ~ "Да",
      stringr::str_to_lower(vasopressors_if_used) == "нет" ~ "Нет",
      TRUE ~ NA_character_
    ),
    vasopressors_used = factor(vasopressors_used, levels = c("Да", "Нет")),

    # first_sorption_dt используем как старт ГС
    HA1_start_dt = first_sorption_dt,

    # Потребность в ЗПТ / ИВЛ
    RRT_required = dplyr::case_when(
      is.na(RRT_start_dt) ~ "Нет",
      is.na(HA1_start_dt) ~ NA_character_,
      HA1_start_dt >= (RRT_start_dt - lubridate::hours(12)) &
        (is.na(RRT_resolution_dt) | HA1_start_dt <= RRT_resolution_dt) ~ "Да",
      TRUE ~ "Нет"
    ),
    MV_required = dplyr::case_when(
      is.na(MV_start_dt) ~ "Нет",
      is.na(HA1_start_dt) ~ NA_character_,
      HA1_start_dt >= (MV_start_dt - lubridate::hours(12)) &
        (is.na(MV_resolution_dt) | HA1_start_dt <= MV_resolution_dt) ~ "Да",
      TRUE ~ "Нет"
    ),

    RRT_required = factor(RRT_required, levels = c("Да", "Нет")),
    MV_required  = factor(MV_required,  levels = c("Да", "Нет"))
  )

# --- Основные характеристики ---
vars_base <- c(
  "first_sorption_type_total",
  "age_yr",
  "sex",
  "icu_to_HA1_hours",
  "SOFA",
  "VIS2020_0_excl",
  "PaFiO2_calc",
  "procalcitonin",
  "C_react_protein",
  "lymphocytes",
  "thrombocytes",
  "lactate",
  "vasopressors_used",
  "RRT_required",
  "MV_required"
)

tbl_base <- pt0 %>%
  select(any_of(vars_base)) %>%
  tbl_summary(
    by = first_sorption_type_total,
    type = list(
      age_yr ~ "continuous",
      icu_to_HA1_hours ~ "continuous",
      SOFA ~ "continuous",
      VIS2020_0_excl ~ "continuous",
      PaFiO2_calc ~ "continuous",
      procalcitonin ~ "continuous",
      C_react_protein ~ "continuous",
      lymphocytes ~ "continuous",
      thrombocytes ~ "continuous",
      lactate ~ "continuous",
      sex ~ "categorical",
      vasopressors_used ~ "dichotomous",
      RRT_required ~ "dichotomous",
      MV_required ~ "dichotomous"
    ),
    value = list(
      vasopressors_used ~ "Да",
      RRT_required ~ "Да",
      MV_required ~ "Да"
    ),
    statistic = list(
      all_continuous() ~ "{median} ({p25}, {p75})",
      all_categorical() ~ "{p}% ({n})"
    ),
    label = list(
      age_yr = "Возраст (лет)",
      sex = "Пол",
      icu_to_HA1_hours = "Время между поступлением в ОРИТ и началом ГС, ч",
      SOFA = "SOFA",
      VIS2020_0_excl = "Индекс VIS2020",
      PaFiO2_calc = "Индекс оксигенации (PaO2/FiO2)",
      procalcitonin = "Прокальцитонин",
      C_react_protein = "C-реактивный белок",
      lymphocytes = "Лимфоциты",
      thrombocytes = "Тромбоциты",
      lactate = "Лактат",
      vasopressors_used = "Потребность в вазопрессорах",
      RRT_required = "Потребность в ЗПТ",
      MV_required = "Потребность в ИВЛ"
    ),
    missing = "no"
  ) %>%
  modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
  bold_labels()

# --- Показания к гемосорбции ---
tbl_indications <- NULL
if ("is_septic" %in% names(pt0)) {
  pt0_septic <- pt0 %>%
    transmute(
      first_sorption_type_total,
      is_septic_cat = factor(
        dplyr::case_when(
          is_septic == 1 ~ "Септические",
          is_septic == 0 ~ "Асептические",
          TRUE ~ NA_character_
        ),
        levels = c("Септические", "Асептические")
      )
    )

  tbl_indications <- pt0_septic %>%
    tbl_summary(
      by = first_sorption_type_total,
      type = list(is_septic_cat ~ "categorical"),
      statistic = list(all_categorical() ~ "{n} ({p}%)"),
      label = list(is_septic_cat = "Септические / асептические"),
      missing = "no"
    ) %>%
    modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
    bold_labels()
}

# --- Патогены ---
tbl_path <- NULL
if (all(c("is_pat_gram_positive", "is_pat_gram_negative") %in% names(pt0))) {
  pt0_path <- pt0 %>%
    mutate(
      pat_any = factor(
        dplyr::case_when(
          is_pat_gram_positive == 1 | is_pat_gram_negative == 1 ~ "Положительный посев",
          is_pat_gram_positive == 0 & is_pat_gram_negative == 0 ~ "Отрицательный посев",
          TRUE ~ NA_character_
        ),
        levels = c("Положительный посев", "Отрицательный посев")
      ),
      pat_gram_positive_slide = factor(
        dplyr::case_when(
          is_pat_gram_positive == 1 ~ "Положительный посев",
          is_pat_gram_positive == 0 ~ "Отрицательный посев",
          TRUE ~ NA_character_
        ),
        levels = c("Положительный посев", "Отрицательный посев")
      ),
      pat_gram_negative_slide = factor(
        dplyr::case_when(
          is_pat_gram_negative == 1 ~ "Положительный посев",
          is_pat_gram_negative == 0 ~ "Отрицательный посев",
          TRUE ~ NA_character_
        ),
        levels = c("Положительный посев", "Отрицательный посев")
      )
    )

  path_vars_present <- intersect(
    c("pat_any", "pat_gram_positive_slide", "pat_gram_negative_slide"),
    names(pt0_path)
  )

  path_vars_nonempty <- path_vars_present[
    vapply(path_vars_present, function(v) any(!is.na(pt0_path[[v]])), logical(1))
  ]

  if (length(path_vars_nonempty) > 0) {
    tbl_path <- pt0_path %>%
      select(first_sorption_type_total, any_of(path_vars_nonempty)) %>%
      tbl_summary(
        by = first_sorption_type_total,
        type = all_categorical() ~ "categorical",
        statistic = all_categorical() ~ "{n} ({p}%)",
        label = list(
          pat_any = "Патогены (общ.)",
          pat_gram_positive_slide = "Грамположительные",
          pat_gram_negative_slide = "Грамотрицательные"
        ),
        missing = "no"
      ) %>%
      modify_table_body(
        ~ .x %>%
          dplyr::filter(
            !(.data$variable == "pat_any" &
                .data$row_type == "level" &
                .data$label == "Отрицательный посев")
          )
      ) %>%
      modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
      bold_labels()
  }
}

# --- Сборка в одну таблицу ---
tbl_list <- list(tbl_base)
hdrs <- c("Исходные по пациентам")

if (!is.null(tbl_indications)) {
  tbl_list <- append(tbl_list, list(tbl_indications))
  hdrs <- c(hdrs, "Показания к гемосорбции")
}

if (!is.null(tbl_path)) {
  tbl_list <- append(tbl_list, list(tbl_path))
  hdrs <- c(hdrs, "Патогены")
}

tbl2_total <- gtsummary::tbl_stack(tbl_list, group_header = hdrs)

# Переносим group headers в один столбец Characteristic
if (!is.null(tbl2_total$table_body) && "groupname" %in% names(tbl2_total$table_body)) {
  tbl2_total$table_body <- tbl2_total$table_body %>%
    dplyr::mutate(
      label = dplyr::if_else(.data$row_type == "group_header", .data$groupname, .data$label)
    ) %>%
    dplyr::select(-groupname)

  if (!is.null(tbl2_total$table_header) && "column" %in% names(tbl2_total$table_header)) {
    tbl2_total$table_header <- tbl2_total$table_header %>%
      dplyr::filter(.data$column != "groupname")
  }
}

flx2_total <- gtsummary::as_flex_table(tbl2_total)
invisible(flextable::save_as_pptx(flx2_total, path = "table2_patients_lps_total.pptx"))
flx2_total
```

## Слайд 3. Данные по сорбциям

```{r}
#| label: pptx_slide3
#| include: true

stopifnot(exists("tbl_ha_gtsummary_compact"))

flx3 <- gtsummary::as_flex_table(tbl_ha_gtsummary_compact) %>%
  flextable::autofit()

invisible(flextable::save_as_pptx(flx3, path = "table3_sorptions.pptx"))
flx3
```

## Слайд 4. Исходы

```{r}

#| label: pptx_slide4
#| include: true

# Здесь собираем таблицу исходов по НОВЫМ точкам,
# которые уже переопределены в analysis_population_by_clin_assess_order
# и сохранены в patients_analysis_clin_window.

sofa_col <- "SOFA"
vis_col  <- "VIS2020_0_excl"
cgi_col  <- "HA_efficency_CGI_E"

tp0 <- "m12h_0h"
tp1 <- "48h_72h"

first_non_na <- function(x) {
  x <- x[!is.na(x) & as.character(x) != ""]
  if (length(x) == 0) NA else x[1]
}

# --- 1 строка на пациента ---
# Берем данные уже из patients_analysis_clin_window,
# то есть из датасета со сдвинутыми таймпойнтами.
out_df <- patients_analysis_clin_window %>%
  group_by(pat_record_id) %>%
  summarise(
    first_sorption_type = first_non_na(first_sorption_type),
    outcome = first_non_na(outcome),
    ICU_in_dt = first_non_na(ICU_in_dt),
    ICU_out_or_death_dt = first_non_na(ICU_out_or_death_dt),
    CGI_E_raw = first_non_na(.data[[cgi_col]]),

    SOFA_m12 = first_non_na(.data[[sofa_col]][timepoint == tp0]),
    SOFA_48_72 = first_non_na(.data[[sofa_col]][timepoint == tp1]),

    VIS_m12 = first_non_na(.data[[vis_col]][timepoint == tp0]),
    VIS_48_72 = first_non_na(.data[[vis_col]][timepoint == tp1]),

    .groups = "drop"
  )

# --- derived outcomes ---
out_df <- out_df %>%
  mutate(
    outcome_norm = norm_txt(outcome),

    # Время до выписки из ОРИТ: только для outcome == "Выписан из ОРИТ"
    time_to_icu_discharge_h = dplyr::if_else(
      outcome_norm == "Выписан из ОРИТ" & !is.na(ICU_in_dt) & !is.na(ICU_out_or_death_dt),
      as.numeric(difftime(ICU_out_or_death_dt, ICU_in_dt, units = "hours")),
      NA_real_
    ),

    # CGI-E: 2 уровня
    CGI_E_2lvl = {
      x <- norm_txt(CGI_E_raw)
      dplyr::case_when(
        x %in% c(
          "Без изменений или хуже",
          "Минимальное улучшение. Незначительное улучшение, не влияющее на клинический статус пациента"
        ) ~ "Нет улучшений",
        x %in% c(
          "Отмечено значительное улучшение. Полная или почти полная ремиссия симптомов",
          "Умеренное улучшение. Частичная ремиссия симптомов"
        ) ~ "Есть улучшения",
        TRUE ~ NA_character_
      ) %>%
        factor(levels = c("Есть улучшения", "Нет улучшений"))
    },

    SOFA_drop_2plus = dplyr::case_when(
      !is.na(SOFA_m12) & !is.na(SOFA_48_72) ~ (SOFA_m12 - SOFA_48_72 >= 2),
      TRUE ~ NA
    ) %>% factor(levels = c(TRUE, FALSE), labels = c("Да", "Нет")),

    VIS_drop_20plus = dplyr::case_when(
      !is.na(VIS_m12) & !is.na(VIS_48_72) ~ (VIS_m12 - VIS_48_72 >= 20),
      TRUE ~ NA
    ) %>% factor(levels = c(TRUE, FALSE), labels = c("Да", "Нет")),

    mortality = dplyr::case_when(
      outcome_norm == "Умер" ~ "Умер",
      !is.na(outcome_norm)   ~ "Не умер",
      TRUE ~ NA_character_
    ) %>% factor(levels = c("Умер", "Не умер"))
  )

tbl4 <- out_df %>%
select(first_sorption_type, time_to_icu_discharge_h, CGI_E_2lvl, SOFA_drop_2plus, VIS_drop_20plus, mortality) %>%
tbl_summary(
  by = first_sorption_type,
  type = list(
    CGI_E_2lvl ~ "dichotomous",
    SOFA_drop_2plus ~ "dichotomous",
    VIS_drop_20plus ~ "dichotomous",
    mortality ~ "dichotomous"
  ),
  value = list(
    CGI_E_2lvl ~ "Есть улучшения",
    SOFA_drop_2plus ~ "Да",
    VIS_drop_20plus ~ "Да",
    mortality ~ "Умер"
  ),
  statistic = list(
    time_to_icu_discharge_h ~ "{median} ({p25}, {p75})",
    all_categorical() ~ "{n} ({p}%)"
  ),
  label = list(
    time_to_icu_discharge_h = "Время до выписки из ОРИТ, ч",
    CGI_E_2lvl = "CGI-E: есть улучшения",
    SOFA_drop_2plus = "Снижение SOFA ≥ 2 за 72 ч",
    VIS_drop_20plus = "Снижение VIS2020 ≥ 20 за 72 ч",
    mortality = "Летальность"
  ),
  missing = "no"
) %>%
  modify_header(all_stat_cols() ~ "**{level}**, N = {n}") %>%
  bold_labels()

flx4 <- gtsummary::as_flex_table(tbl4) %>%
  flextable::autofit()

invisible(flextable::save_as_pptx(flx4, path = "table4_outcomes.pptx"))
flx4
```

```{r}
#| label: qc_pptx_slide4_counts
#| cache: false
#| echo: false

library(dplyr)
library(tidyr)

tp0 <- "m12h_0h"
tp1 <- "48h_72h"

# 1) Сколько строк и пациентов в исходном patients_analysis_clin_window
qc_rows <- patients_analysis_clin_window %>%
  summarise(
    n_rows = n(),
    n_patients = n_distinct(pat_record_id)
  )

qc_pair_status <- patients_analysis_clin_window %>%
  distinct(pat_record_id, timepoint) %>%
  count(pat_record_id, name = "n_timepoints") %>%
  count(n_timepoints, name = "n_patients")

# 2) Сколько пациентов попадает в out_df по группам
qc_out_df <- patients_analysis_clin_window %>%
  group_by(pat_record_id) %>%
  summarise(
    first_sorption_type = first_non_na(first_sorption_type),
    SOFA_m12 = first_non_na(SOFA[timepoint == tp0]),
    SOFA_48_72 = first_non_na(SOFA[timepoint == tp1]),
    VIS_m12 = first_non_na(VIS2020_0_excl[timepoint == tp0]),
    VIS_48_72 = first_non_na(VIS2020_0_excl[timepoint == tp1]),
    .groups = "drop"
  ) %>%
  mutate(
    has_SOFA_pair = !is.na(SOFA_m12) & !is.na(SOFA_48_72),
    has_VIS_pair  = !is.na(VIS_m12) & !is.na(VIS_48_72)
  ) %>%
  group_by(first_sorption_type) %>%
  summarise(
    n_patients_total = n(),
    n_with_SOFA_pair = sum(has_SOFA_pair, na.rm = TRUE),
    n_with_VIS_pair  = sum(has_VIS_pair, na.rm = TRUE),
    .groups = "drop"
  )

qc_rows
qc_pair_status
qc_out_df
```


## Слайд 5. Графики

```{r}

#| label: plots_m12h_0h_48h_72h facet_wrap
#| cache: true
#| warning: false
#| message: false
#| fig-width: 12
#| fig-height: 8

library(dplyr)
library(tidyr)
library(ggplot2)
library(ggh4x)

# Предполагается, что выше уже создан patients_analysis_clin_window
tp_keep <- c("m12h_0h", "48h_72h")

# Подписи временных точек для оси X
tp_labels <- c(
  m12h_0h   = "-12-0 часов",
  `48h_72h` = "48-72 часа"
)

# Словарь short -> ru
var_label_map <- dict_pat %>%
  distinct(short, ru) %>%
  filter(!is.na(short), short != "", !is.na(ru), ru != "") %>%
  { setNames(.$ru, .$short) }

# Ручные подписи для отдельных переменных
manual_labels <- c(
  PaFiO2_calc    = "PaO2/FiO2 (расч.)",
  VIS2020_0_excl = "Индекс VIS2020"
)

lab_var <- function(v) {
  v <- as.character(v)

  lbl_manual <- unname(manual_labels[v])
  lbl_dict   <- unname(var_label_map[v])

  lbl <- ifelse(!is.na(lbl_manual) & lbl_manual != "", lbl_manual, lbl_dict)
  lbl[is.na(lbl) | lbl == ""] <- v[is.na(lbl) | lbl == ""]
  lbl
}

# Какие переменные рисуем
vars_facet_keep <- c("SOFA", "VIS2020_0_excl", "PaFiO2_calc", "lactate")
vars_plot <- intersect(vars_facet_keep, names(patients_analysis_clin_window))

# Для каких переменных нужна логарифмическая ось Y
vars_log_Y <- c("lactate", "VIS2020_0_excl")

# Берем только пациентов с first_sorption_type == "LPS"
plot_source <- patients_analysis_clin_window %>%
  filter(first_sorption_type == "LPS") %>%
  select(pat_record_id, timepoint, any_of(vars_plot))

plot_long <- plot_source %>%
  pivot_longer(
    cols = -c(pat_record_id, timepoint),
    names_to = "variable",
    values_to = "value"
  ) %>%
  mutate(
    timepoint = factor(timepoint, levels = tp_keep),
    variable_chr = as.character(variable),
    value_raw = as.numeric(value),
    value_plot = value_raw,
    variable_label = lab_var(variable_chr),
    variable_facet = dplyr::case_when(
      variable_chr == "VIS2020_0_excl" ~ paste0(variable_label, " (log10-axis)"),
      variable_chr == "lactate" ~ paste0(variable_label, " (log10-axis)"),
      TRUE ~ variable_label
    )
  ) %>%
  filter(!is.na(value_plot))

# Оставляем только полные пары для каждой переменной
plot_long_paired <- plot_long %>%
  group_by(variable_facet, pat_record_id) %>%
  filter(n_distinct(timepoint) == 2, n() == 2) %>%
  ungroup()

n_pairs <- plot_long_paired %>%
  distinct(pat_record_id) %>%
  nrow()

plot_lines <- plot_long_paired %>%
  group_by(variable_facet, pat_record_id) %>%
  filter(n_distinct(timepoint) == 2, n() == 2) %>%
  ungroup()

# p-value по Wilcoxon для каждой панели
pvals_df <- plot_long_paired %>%
  group_by(variable_facet) %>%
  summarise(
    p_txt = {
      w <- cur_data() %>%
        select(pat_record_id, timepoint, value_raw) %>%
        pivot_wider(names_from = timepoint, values_from = value_raw)

      if (
        nrow(w) >= 2 &&
        all(tp_keep %in% names(w)) &&
        sum(!is.na(w[[tp_keep[1]]]) & !is.na(w[[tp_keep[2]]])) >= 2
      ) {
        pval <- suppressWarnings(
          wilcox.test(
            x = w[[tp_keep[2]]],
            y = w[[tp_keep[1]]],
            paired = TRUE,
            exact = FALSE
          )$p.value
        )
        paste0("p = ", format.pval(pval, digits = 2, eps = 1e-3))
      } else {
        "p = NA"
      }
    },
    .groups = "drop"
  )

log_facets <- c(
  paste0(lab_var("VIS2020_0_excl"), " (log10-axis)"),
  paste0(lab_var("lactate"), " (log10-axis)")
)

p <- ggplot(plot_long_paired, aes(x = timepoint, y = value_plot)) +
  geom_boxplot(aes(fill = timepoint), outlier.shape = NA, alpha = 0.55) +
  geom_line(data = plot_lines, aes(group = pat_record_id), alpha = 0.20) +
  geom_point(
    position = position_jitter(width = 0.008, height = 0),
    size = 0.9,
    alpha = 0.35
  ) +
  facet_wrap(~ variable_facet, scales = "free_y", ncol = 2) +
  scale_x_discrete(labels = tp_labels) +
  geom_text(
    data = pvals_df,
    aes(x = tp_keep[2], y = Inf, label = p_txt),
    inherit.aes = FALSE,
    hjust = 1.05,
    vjust = 1.2,
    size = 3
  ) +
  ggh4x::facetted_pos_scales(
    y = list(
      variable_facet %in% log_facets ~ scale_y_log10(
        expand = expansion(mult = c(0.1, 0.1))
      ),
      TRUE ~ scale_y_continuous(
        expand = expansion(mult = c(0.05, 0.1))
      )
    )
  ) +
labs(
  x = NULL,
  y = NULL,
  title = "Парные сравнения (только LPS)",
  subtitle = paste0("Число пациентов с полными парами: n = ", n_pairs)
) +
  theme_bw() +
  theme(legend.position = "none")

p

# Сохраняем именно текущий график p в PNG,
# который потом будет вставлен в PowerPoint
ggsave(
  filename = "slide5_facets.png",
  plot = p,
  width = 13.33,
  height = 7.5,
  units = "in",
  dpi = 300
)

# Создаём PowerPoint с одним слайдом, на котором график занимает всю область слайда
ppt <- officer::read_pptx()
lsum <- officer::layout_summary(ppt)

ppt <- officer::add_slide(
  ppt,
  layout = lsum$layout[[1]],
  master = lsum$master[[1]]
)

ppt <- officer::ph_with(
  x = ppt,
  value = officer::external_img("slide5_facets.png", width = 13.33, height = 7.5),
  location = officer::ph_location_fullsize()
)

print(ppt, target = "slide5_facets.pptx")
```
